use crystal_forge::compliance::canonical::{ImplementationState, PublicationState};
use crystal_forge::compliance::xccdf::export_models::{XccdfBundleExport, XccdfPolicyExport};
use crystal_forge::compliance::xccdf::xml_writer::write_bundle_xccdf_export;
use serde_json::json;
use uuid::Uuid;

fn main() {
    let bundle_id = Uuid::parse_str("aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa").unwrap();
    let bundle_version_id = Uuid::parse_str("bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb").unwrap();
    let policy_id = Uuid::parse_str("11111111-1111-1111-1111-111111111111").unwrap();
    let policy_version_id = Uuid::parse_str("22222222-2222-2222-2222-222222222222").unwrap();

    let snapshot = XccdfBundleExport {
        bundle_id,
        bundle_version_id,
        version: "2.3.0".into(),
        publication_state: PublicationState::Accepted,
        semantic_digest: "abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789".into(),
        name: "Crystal Forge Export Fixture".into(),
        description: Some("Generated by write_bundle_xccdf_export".into()),
        framework: "NIST SP 800-53".into(),
        framework_version: Some("5.1".into()),
        layer: "nixos".into(),
        owner: "crystal-forge".into(),
        policies: vec![XccdfPolicyExport {
            policy_id,
            policy_version_id,
            version: "4.2.0".into(),
            publication_state: PublicationState::Accepted,
            semantic_digest: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
                .into(),
            name: "Require Crystal Forge Agent".into(),
            description: Some("The Crystal Forge agent must be installed.".into()),
            policy_type: "require_cf_agent".into(),
            execution_phase: "nix-evaluation".into(),
            implementation_state: ImplementationState::Native,
            enabled_default: true,
            selected: true,
            policy_order: 1,
            config: json!({"strict": true}),
            compliance_metadata: json!({
                "identifiers": [{"system": "https://crystal-forge.org/ids", "value": "CF-001"}],
                "references": [{"href": "https://crystal-forge.org/docs/agent", "title": "Agent documentation"}]
            }),
            dependencies: json!([]),
            opaque_xml: None,
            source_mappings: vec![],
        }],
    };

    match write_bundle_xccdf_export(&snapshot) {
        Ok(xml) => print!("{xml}"),
        Err(error) => {
            eprintln!("failed to generate XCCDF fixture: {error}");
            std::process::exit(1);
        }
    }
}
