[application]
name = "crystal-forge-ui"
default_platform = "web"

[web.app]
title = "Crystal Forge"

[web.watcher]
reload_html = true
watch_path = ["src"]

[web.resource]
style = []

[web.resource.dev]
style = []

# Note: Proxy configuration is not supported in Dioxus 0.7.3
# Instead, the API client (src/api/client.rs) detects dev server ports
# and automatically redirects to http://localhost:3000 for the backend
