---
id: TASK-440
title: Bring system configuration and flake exploration into full design parity
status: In Progress
assignee:
  - '@openai-agent'
created_date: '2026-08-28 03:43'
updated_date: '2026-08-31 08:47'
labels:
  - design-parity
  - web-ui
  - backend
  - systems
  - flakes
dependencies: []
references:
  - git commit eb5a18513623890e9dac1e8a74565078243288a8
  - git parent cfae4f4a33815c72059d309a547672f8c9039747
  - 'https://gitlab.com/crystal-forge/crystal-forge/-/merge_requests/318'
  - origin/TASK-433-policy-poam-workflows
  - 'https://gitlab.com/crystal-forge/crystal-forge/-/merge_requests/323'
  - git commit cafc678ef
  - git commit 1b9a0594193a26da99b0935151b64323acf8f913
  - git commit 4e09d60a
documentation:
  - docs/design/CrystalForge/app.jsx
  - docs/design/CrystalForge/components/SystemDetail.jsx
  - docs/design/CrystalForge/components/FlakeExplorer.jsx
  - docs/design/CrystalForge/components/FlakesView.jsx
  - docs/design/CrystalForge/components/EnvironmentsView.jsx
  - docs/design/CrystalForge/components/AddSystemModal.jsx
  - docs/design/CrystalForge/components/Shell.jsx
  - docs/design/CrystalForge/components/ComplianceView.jsx
  - docs/design/CrystalForge/data-config.js
  - docs/design/CrystalForge/data-flake-explorer.js
  - docs/design/CrystalForge/data-flakes.js
  - docs/design/CrystalForge/styles.css
modified_files:
  - packages/web-ui/src/views/system_detail.rs
  - packages/web-ui/src/views/flakes_list.rs
  - packages/web-ui/src/views/environments_list.rs
  - packages/web-ui/src/views/compliance.rs
  - packages/web-ui/src/components/layout/topbar.rs
  - packages/web-ui/src/state/navigation_focus.rs
  - packages/web-ui/src/api/models.rs
  - packages/web-ui/src/api/client.rs
  - packages/web-ui/assets/app.css
  - packages/default/crates/cf-server/src/
  - packages/default/crates/cf-builder/src/
  - packages/default/crates/cf-protocol/src/
  - checks/web-ui/
priority: high
type: feature
ordinal: 449000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Implement the complete Rust frontend and backend product behavior represented by design commit `eb5a18513623890e9dac1e8a74565078243288a8` (compared with parent `cfae4f4a33815c72059d309a547672f8c9039747`). The design commit is the authoritative visual and interaction reference; fixture randomness and the changed `.thumbnail` are not product requirements.

System Config scope:
- Replace the static rendered-module presentation with a revision-aware Evaluated options explorer.
- Support Generations and Commits modes, current/historical/never-deployed revision context, reload-safe deep links, and Back to current.
- Provide server-backed debounced search, All/Overridden/Changed filters with counts, bounded pagination, stale-response protection, loading/empty/error states, and a table aligned with the Modules/Evaluation/Drift cards without an unintended inner scroller.
- Show expandable option details with declared type, before/after change, complete winning and overridden definition provenance, source input/revision/path, and winner notes.
- Render scalar, package, structured list/attribute-set, submodule, opaque/function, and failed-evaluation values safely and according to declared type. Unknown or failed values must not be fabricated.
- Add real Modules and Evaluation summary cards plus the read-only module source tray. Tracked provenance can open the relevant flake/revision; untracked provenance remains visibly unavailable.
- Keep Drift behavior accurate and add the Overview store-path link into the current evaluation.

Evaluation lifecycle and comparison semantics:
- Config reads never launch Nix evaluation. Evaluation extraction occurs in the existing authorized evaluation/build job path and produces a reusable snapshot.
- When a requested never-deployed revision lacks a snapshot, Config shows an explicit unavailable state. An authorized explicit action may queue a new evaluation job or reuse an already queued/running job; the read request itself remains side-effect free. The UI distinguishes unavailable, queued, running, failed, and available states.
- In Generations mode, Changed compares the selected generation with the immediately preceding retained generation that has an evaluation snapshot. In Commits mode, Changed compares the selected commit with its Git first-parent snapshot for the same configuration. When no valid baseline snapshot exists, the UI says no comparison is available and does not report a zero-change result.
- Evaluation data required by a retained deployment generation remains queryable for as long as that generation is retained. An arbitrary commit snapshot remains queryable for as long as its flake timeline record is retained. Nix store garbage collection and branch rewrite/deletion must not silently remove retained snapshot metadata; genuinely missing or corrupt artifacts produce an explicit unavailable state.

Flake drawer scope:
- Add revision-scoped Commits, Systems, Modules, and Inputs tabs with counts and alert states.
- Make the selected commit govern every output pane and show revision identity plus host/module/input deltas against its Git first parent. Root commits and commits whose parent snapshot is unavailable show No previous revision rather than fabricated zero deltas.
- Reconcile declared configurations with managed systems, including managed, declared-but-unmanaged, and managed-but-undeclared states; include warnings for output collapse and systems pinned to older revisions.
- Let managed rows open that system's Config tab at the selected revision and unmanaged rows open system registration prefilled with configuration/hostname, flake, and branch.
- Show exported modules, declared options, consumer/blast-radius counts, and expandable declaration details.
- Show direct and resolved inputs, lock revisions, source, age, follows/transitive data, tracked/channel state, revision bumps, stale inputs, and multiple nixpkgs revision warnings.
- Derive all flake system totals consistently from the authoritative managed-system relationship across subtitles, lists, cards, rollout displays, and removal warnings.

Cross-surface and smaller parity scope:
- Make environment flake chips open the flake and restore the originating environment panel when the drawer closes.
- Preserve Config-to-flake provenance navigation and Flake-to-Config revision context.
- Deep-link pending deployment approval notifications to the exact system Deploy tab.
- Remove duplicate System Detail header Deploy/Rollback actions; route History rollback into Deploy with the exact previous generation selected; rename selectors to New commit and Previous generation.
- Add the `auto_latest` manual-deployment warning with Cancel, Continue on auto_latest, and Convert to manual and deploy outcomes, with truthful persisted/result state and idempotent retries.
- Remove only the duplicate inner Compliance bundle Edit action while retaining the authorized outer action.
- Ensure the file diff modal layers and receives input above the flake tray.
- Use full immutable commit SHA identity for persisted snapshots, API keys, caches, comparisons, navigation state, and row identity. Abbreviated SHAs are presentation only.

Backend, storage, and security outcome:
- Expose authorized, revision-specific, cached system evaluation data and revision-scoped flake output data required by these views.
- Evaluation data must come from the NixOS options tree rather than serializing `config`; retain declared types, safe values/errors, definition provenance, package metadata, module origins, evaluation identity/metrics, and comparison data.
- Search/filter/pagination and cross-revision diffs are computed server-side with bounded responses rather than transferring complete snapshots to the browser.
- Flake snapshots cover declared systems, exported modules and declarations/consumers, resolved lock inputs, and first-parent deltas without triggering per-host work while browsing.
- Snapshot persistence must provide content deduplication and bounded storage amplification equivalent to the design's content-addressed base-plus-delta intent. It must not store a separate complete multi-thousand-option corpus for every host and revision when that content is shared.
- Secret redaction occurs before unsafe data is persisted, indexed, diffed, logged, or serialized by an API. The boundary covers option values, nested/submodule values, package/collection elements, module defaults, evaluation errors, source metadata, and repository URLs containing credentials or tokens. Redacted data is neither returned nor searchable; masking only in Dioxus rendering is insufficient.
- Preserve environment visibility, existing mutation authorization, and deployed agent/builder compatibility. Hidden environments and unauthorized revisions use the application's existing non-disclosing authorization behavior.

Deep-link and interaction contract:
- The exact system identity, Config tab, revision mode, and full SHA survive hard reload and browser back/forward navigation. Unknown revisions and unavailable snapshots render explicit states; unauthorized or hidden targets do not disclose protected existence.
- A thorough UI/UX pass is part of this task. Every affected Rust surface matches the design example in the authoritative browser environment at 1920x1080 in light and dark themes, with narrow behavior exercised at 900x900. Keyboard assertions cover tab navigation, row expansion, revision controls, Escape behavior, modal focus trapping, and focus restoration.

The `modifiedFiles` metadata is anticipated and non-exhaustive. The implementation may modify the existing routing, modal, API, persistence, protocol, test, or migration files that actually own the required behavior, while remaining within this task's product scope.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The System Config tab defaults to the current deployed generation and supports generation, commit, reload-safe deep-linked revision, historical warning, never-deployed warning, unavailable local generation, and Back to current states using real revision-specific evaluation data.
- [x] #2 Config read endpoints never launch Nix evaluation. A missing never-deployed snapshot is explicitly unavailable until an authorized evaluation action queues or reuses a job, and the UI distinguishes unavailable, queued, running, failed, and available states.
- [x] #3 In Generations mode Changed compares against the immediately preceding retained generation with an evaluation snapshot; in Commits mode it compares against the selected commit's Git first-parent snapshot for the same configuration; a missing baseline displays no comparison available rather than zero changes.
- [x] #4 Evaluation artifacts required by retained deployment generations remain queryable for the generation lifetime, arbitrary commit snapshots remain queryable for the flake timeline-record lifetime, and store GC or branch rewrite/deletion does not silently erase retained snapshot metadata.
- [x] #5 Evaluated-options search is debounced and server-side across option path, safe rendered value, declared type, safe evaluation error, source path, and source input; redacted text is not searchable; filter counts remain revision-global while the result total reflects the active search and filter.
- [x] #6 Evaluated options provide All/Overridden/Changed filters, bounded pagination, correct range/button/count behavior, loading/empty/unavailable/unauthorized/error states, and protection against older asynchronous responses overwriting a newer request.
- [x] #7 Expandable option rows show declared type, the mode-defined comparison, complete winning and overridden definitions, source input/revision/path, and definition status without fabricating unknown or failed values.
- [x] #8 Scalar, package, structured collection, submodule, opaque/function, and failed-evaluation values and diffs render safely and type-appropriately, including package additions/removals and explicit not-evaluated errors.
- [x] #9 The Config Modules, Evaluation, Drift, and source-tray surfaces use real selected-revision data; tracked provenance opens the correct flake/revision, untracked provenance is non-navigable, and the Overview evaluation link opens the current revision.
- [x] #10 Secret-bearing option values, nested values, collection/package elements, module defaults, evaluation errors, source metadata, and credential-bearing repository URLs are redacted before persistence, indexing, diffing, logging, or API serialization and cannot be recovered through API responses or search.
- [x] #11 Snapshot persistence deduplicates shared evaluation content and bounds storage amplification so complete multi-thousand-option corpora are not independently duplicated for every host and revision where the content is shared.
- [x] #12 The flake drawer provides Commits, Systems, Modules, and Inputs tabs whose counts, alerts, contents, selected full revision identity, and Git first-parent deltas update consistently when the selected commit changes.
- [x] #13 A flake root commit or missing first-parent snapshot displays No previous revision and no fabricated delta; full SHAs key APIs, snapshots, caches, navigation, comparisons, and row identity, with a test proving two commits sharing a displayed SHA prefix remain distinct.
- [x] #14 The flake Systems pane correctly classifies managed, declared-but-unmanaged, and managed-but-undeclared systems; shows output-collapse and pinned-revision warnings; opens managed systems at Config for the exact selected revision; and prefills registration for unmanaged declarations.
- [x] #15 The flake Modules pane shows exported modules, descriptions, declaration counts, consumer/blast-radius counts, and expandable option path/type/default details from cached revision data.
- [x] #16 The flake Inputs pane shows direct and resolved counts, source and locked revision, update age, follows/transitive details, tracked/channel state, bumps, stale-over-90-days state, and multiple-nixpkgs-revision warnings.
- [x] #17 All flake system counts shown in page subtitles, table rows, cards, rollout denominators, drawer metrics, and removal warnings reconcile to one authoritative managed-system relationship.
- [x] #18 The exact system, Config tab, revision mode, and full SHA survive hard reload and browser back/forward; unknown revisions and unavailable snapshots render explicit states; unauthorized or hidden targets follow existing non-disclosing authorization behavior.
- [x] #19 Environment-to-flake navigation restores the originating environment panel on close; Config provenance opens the correct tracked flake/revision; Flake Systems opens Config at the selected revision; and unrelated later navigation does not retain stale context.
- [x] #20 Pending deployment approval notifications open the exact system on Deploy; System Detail has no duplicate header Deploy/Rollback controls; History rollback opens Deploy with the exact generation selected; and selector labels read New commit and Previous generation.
- [x] #21 Manual deployment from an auto_latest system offers Cancel, Continue on auto_latest, and Convert to manual and deploy; failed conversion queues no deployment; successful conversion followed by deployment failure reports the persisted manual policy and failed deployment; retries do not silently create duplicate deployments.
- [x] #22 Only the duplicate inner Compliance bundle edit action is removed, the authorized outer edit action remains, and file diff modals render and receive interaction above the flake tray.
- [x] #23 Authoritative browser coverage includes Config snapshot unavailable and API error, flake snapshot unavailable and API error, no previous flake revision, unauthorized/hidden environment, current/historical/never-deployed Config, expanded typed diffs, evaluation error, tracked/untracked provenance, all flake panes, cross-navigation, rollback, auto_latest, notification, modal-layering, and compliance states.
- [ ] #24 At the authoritative 1920x1080 viewport in light and dark themes and the 900x900 narrow viewport, affected states match commit eb5a1851 with no clipping, overlap, incorrect stacking, inaccessible controls, unintended inner scrolling, or table/card misalignment; assertions verify text, counts, revisions, warnings, actions, and navigation.
- [x] #25 Keyboard coverage verifies tab order/navigation, row expansion, revision controls, drawer and modal Escape behavior, modal focus trapping, and focus restoration after closing drawers and modals.
- [x] #26 New evaluation and flake-output APIs provide bounded server-side query/diff results, preserve supported agent/builder compatibility and environment authorization, avoid evaluation side effects on read paths and per-host evaluation during flake browsing, and return explicit unavailable/error states.
- [x] #27 Targeted frontend and server tests, security/redaction tests, snapshot lifecycle and deduplication tests, auto_latest failure/idempotency tests, SQLx metadata/schema checks when applicable, the web-ui package build, the authoritative web-ui check, and broader Nix flake checks required by protocol, migration, packaging, or cross-package changes pass in the repository Nix development environment.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Implementation plan (recorded 2026-08-28)

### Coordination and sequencing
- Keep TASK-440 on its dedicated branch/worktree and track TASK-433 through MR !318 / `origin/TASK-433-policy-poam-workflows`.
- Do not edit TASK-433 migrations. Re-fetch its branch before allocating any TASK-440 migration; TASK-433 currently owns 0233 and 0234 and may add more. Update TASK-440 onto the merged TASK-433 result before TASK-440 is eligible to merge.
- Prefer additive files/helpers and isolated flakes/config work while TASK-433 is active. Defer conflict-prone final edits in System Detail, Compliance, notifications, shared DTOs/CSS, and browser manifests until the latest TASK-433 head is reconciled.

### Phase 1 — Identity, navigation, and overlay foundations
- Make full commit SHA the sole identity for flake rows, selection, keys, comparisons, and navigation; abbreviation remains presentation only. Add a collision regression for two SHAs sharing the displayed prefix.
- Introduce typed URL-backed navigation state for System Detail tab/config revision and flake drawer pane/revision/return context, with round-trip/back-forward/stale-context tests.
- Establish explicit overlay ordering and focus behavior (page < tray/drawer < modal < toast), then fix the file-diff modal stacking, Escape order, focus trap, and focus restoration.

### Phase 2 — Secure snapshot domain and extraction
- Add reusable pre-persistence redaction for nested option values, package/collection elements, defaults, evaluation errors, provenance/source metadata, and credential-bearing URLs. Build search text and content digests only from already-redacted representations.
- Define tagged safe option values/errors/provenance and revision/snapshot lifecycle states without serializing `config` or fabricating failed/opaque values.
- Extend the existing authorized bulk evaluation path to extract target `options` data, module/evaluation summaries, and one per-commit flake output/lock snapshot; Config and flake read paths remain database-only and never invoke Nix/Git/network work.
- Preserve builder/agent protocol compatibility unless verification proves a protocol addition is unavoidable.

### Phase 3 — Additive persistence and lifecycle
- After refreshing TASK-433 migration allocation, add content-addressed snapshot/content tables and lightweight per-revision/configuration option references, explicit first-parent identity, flake output systems/modules/inputs, and durable retained-generation references.
- Use full `(flake, SHA, configuration)` identities, canonical redacted digests, bounded indexes, and restrictive retention semantics so store GC/history rewrites do not silently remove snapshots needed by retained generations/timeline records.
- Add isolated-PostgreSQL tests for deduplication, bounded amplification, retention, corruption/missing states, first-parent/generation baselines, and prefix collisions; refresh SQLx metadata only after schema/query stabilization.

### Phase 4 — Authorized bounded APIs
- Add non-disclosing environment-aware Config summary/options/detail and flake output APIs with explicit unavailable/queued/running/failed/available/error states.
- Compute search/filter/count/pagination and generation/first-parent comparisons server-side. Clamp requests and keep filter counts revision-global while result totals reflect active query/filter.
- Add an authorized idempotent explicit evaluation queue/reuse action; secure the existing re-evaluation path and prove all reads are side-effect free.
- Centralize authoritative managed-system reconciliation/counts for all flake surfaces.

### Phase 5 — Config and flake explorer UI
- Replace static Config with URL-backed generation/commit controls, lifecycle and comparison states, debounced stale-safe server queries, filters/counts/pagination, typed expandable rows, provenance/source tray, and real Modules/Evaluation/Drift cards.
- Add revision-scoped flake Commits/Systems/Modules/Inputs panes, first-parent/no-previous states, reconciliation/warnings, exact Config navigation, prefilled registration, modules/consumers, and resolved input details.
- Port only the needed design layout behavior into Rust CSS, including 1920x1080 light/dark and 900x900 behavior with no unintended inner scrolling.

### Phase 6 — Cross-surface parity
- Restore environment panels after flake navigation; preserve Config↔flake exact revision context and clear stale context after unrelated navigation.
- Route approval notifications to the exact system Deploy tab; remove duplicate System Detail header actions; route History rollback into Deploy with exact generation; rename selectors.
- Implement truthful idempotent auto_latest Cancel/Continue/Convert-and-deploy outcomes against a server contract.
- Remove only the duplicate inner Compliance Edit while preserving authorization and the outer action.

### Phase 7 — Verification and TASK-433 reconciliation
- Rebase/update onto merged TASK-433, resolve shared surfaces without regressing POA&M behavior, then rerun schema/SQLx preparation.
- Add targeted Rust/server/security/lifecycle/API tests and authoritative browser workflows for every AC state, including delayed response races, unavailable/error/non-disclosure, typed diffs, cross-navigation, deployment outcomes, layering, and keyboard focus.
- Exercise affected canonical states at 1920x1080 dark/light and 900x900 narrow, enroll TASK-440 workflows as critical, retain screenshots, and run the applicable package builds, web-ui check, integration/server-regression checks, and `nix flake check --keep-going`.

Frontend-only Phase 1 execution slice (2026-08-28): keep Dioxus route variants unchanged to minimize conflict with TASK-433, and add typed query parsing/serialization in `state/navigation_focus.rs`. Bind System Detail tab, Config revision mode/full SHA or generation, Deploy generation, and flake drawer/pane/full revision/return-environment context to browser history with popstate synchronization and stale-context replacement. Preserve exact full SHA identity in state and tests. Add flake tray/diff modal Escape precedence, focus trapping, initial focus, and focus restoration. Apply isolated AC #20/#22 fixes in System Detail, topbar notifications, selector text, and Compliance while preserving TASK-433 POA&M authorization semantics. Verify with focused web-ui Rust tests, package tests where feasible, formatting for touched files, and `git diff --check`.

Backend implementation slice (2026-08-28): refreshed `origin/TASK-433-policy-poam-workflows` at `fd7548ad`; it still owns only migrations 0233/0234 while `origin/dev` ends at 0232, so allocate additive TASK-440 migration 0235. Add documented tagged snapshot domain types and pre-persistence redaction/canonical digests; extend the existing bulk evaluation expression/result path to collect options-tree and flake-output metadata; persist content-addressed option payloads and revision/configuration references atomically with successful evaluation finalization; add environment-scoped database-only lifecycle/options/flake snapshot reads with bounded search/filter/count/pagination/diff; add an authorized idempotent queue/reuse action; centralize managed-system reconciliation; verify focused domain/query/handler tests with SQLX_OFFLINE=true. Do not touch TASK-433 migrations, web-ui, or browser tests.

Frontend contract-consumption slice (2026-08-28): own only `packages/web-ui`; preserve concurrent backend, migration, TASK-433 POA&M, and browser-test changes. Mirror `evaluation_snapshots.rs` DTOs and add bounded client methods. Replace static Config with URL-selected revision lifecycle, explicit queue action, debounced stale-safe server query, global counts/filter/pagination, typed values/diffs/provenance, source tray, and selected-revision summary/drift cards. Connect flake Commits/Systems/Modules/Inputs tabs to revision output snapshots with full-SHA identity, lifecycle/no-parent states, authoritative reconciliation/counts, exact Config and registration navigation, and truthful rendering of only fields supplied by the server payload. Add focused Rust tests and CSS for wide/narrow layouts; verify full web-ui tests and wasm check. Do not edit browser integration JavaScript.

AC #21 implementation slice (2026-08-28): extend the existing system deploy endpoint with typed deploy actions and typed policy/conversion/deployment outcome state. Add an atomic policy conversion query that commits before deployment is attempted, plus serialized pending-deployment creation that reuses an existing pending target on retry. Preserve current auth, environment non-disclosure, CSRF client use, and audit recording. Add an isolated auto_latest prompt/reducer component with Cancel, Continue on auto_latest, and Convert to manual and deploy, then integrate it into the active System Detail Deploy tab after re-reading concurrent edits. Add pure policy/reducer tests and migrated-PostgreSQL query tests for conversion failure/no queue, partial failure state, and retry deduplication. Run focused SQLX_OFFLINE cf-server and web-ui tests; do not change migrations or browser integration-test.js.

Authoritative browser slice (2026-08-28): own `checks/web-ui`, `checks/ui-screenshots` only if required, and minimal frontend accessibility hooks. Extend the existing manifest-driven `integration-test.js` harness with deterministic routed fixtures for all AC #23-25 Config, flake, cross-surface, deployment, layering, authorization, responsive/theme, and keyboard states. Replace stale duplicate-header assertions, use role/label locators where practical, avoid new arbitrary sleeps, and make every TASK-440 workflow merge-blocking in `checks/web-ui/default.nix`. Update coverage/design fixtures consistently, run Node syntax plus manifest parity validation, focused frontend tests/checks for any accessibility edits, and as much focused authoritative Nix web-ui execution as practical. Preserve all concurrent worktree changes and do not commit or push.

Backend mandatory review-remediation pass (2026-08-28): treat the user's ordered punch list as numbered issues 1-17. Restrict writes to `packages/default/crates/cf-server` and migration `0235`; preserve all frontend/browser changes and migrations 0233/0234. First harden the redaction/evaluator boundary and sanitize all evaluator diagnostics. Then repair authorization/visibility, first-parent and rewrite retention semantics, provenance/generation/failure lifecycle, bounded output/search and one-pass extraction, safe forcing and complete flake metadata/metrics/counts. Finally harden auto_latest transaction/idempotency compatibility and snapshot schema version/corruption/orphan recovery. Add nonignored pure tests plus isolated PostgreSQL tests, run SQLX_OFFLINE checks/tests and isolated migrated-DB tests where available, inspect the final backend-only diff for documentation and source-contract quality, and return evidence mapped to each numbered issue.

Backend follow-up audit remediation (2026-08-28): preserve all `packages/web-ui` and `checks/web-ui` changes and own only cf-server backend plus migration 0235. Address the user's 12 concrete findings by: enforcing declaration-path-aware redaction and pre-log evaluator sanitization; tightening commit-wide reevaluation authorization; applying visibility/bounds/corruption contracts to snapshot APIs; retaining referenced snapshots across source reset and unambiguous generation identity; hardening branch-specific bounded Nix extraction and exported-module forcing; completing summary/provenance metadata; adding periodic orphan reclamation and content ceilings; making auto_latest request-idempotency and policy/queue/audit behavior transactionally truthful while preserving omitted-action rejection; and adding focused unit, isolated PostgreSQL, real-Nix-if-feasible, cross-endpoint consistency, and contract documentation tests. Update only migration 0235, then run focused SQLX_OFFLINE checks/tests and isolated migrated-database tests. Do not commit or push.

Final eight-defect backend remediation (2026-08-29): preserve all frontend/check files and modify only cf-server backend plus migration 0235 if schema support is required. Sanitize evaluation failures at entry before logs/persistence/broadcast and extend recursive diagnostics/URL redaction for ssh, git+https, scp-style credentials, bearer/token text, and lock metadata. Make evaluator traversal complete for options/configurations/modules while bounding only rendered values and API pages; classify before forcing and convert per-value or whole-snapshot size failures into explicit safe opaque/failed/unavailable records without aborting commit finalization. Bind reciprocal generation backfill to pending deployment commit identity, archive retained commits across source resets so generation reads remain valid but active revision authorization rejects old lineage, and enforce immutable full-SHA deployment identity. Use time-bounded implicit retry keys while preserving durable explicit request IDs, and split policy conversion from deployment queue persistence so conversion commits independently and a later deployment failure returns persisted manual state. Add focused pure and isolated PostgreSQL regressions for all eight defects, then run SQLX_OFFLINE compile/tests, migrated isolated-DB tests, formatting, documentation/diff inspection, and no frontend/check edits.

Final backend closure (2026-08-29): normalize option-path components across camelCase, snake_case, kebab-case, and common secret aliases before redaction; add pure regressions. Make omitted deployment request IDs a durable implicit retry intent keyed by existing system/commit/action context, with a new explicit request ID as the backward-compatible boundary for intentional redeployment. Persist and immutably compare request action so reuse of an explicit ID with a different commit or action returns a typed HTTP conflict instead of the prior deployment ID; add query/handler and migrated-DB tests. Replace silently truncated list/attribute-set values with explicit opaque over-limit values and test generated extraction plus model semantics. Emit flake-output metadata through an independent synthetic evaluator output rather than the first NixOS configuration, covering zero configurations and a failing first configuration. Add source_archived=false to every active system/dashboard/flake commit listing and DB regressions. Run focused redaction/model/expression tests, SQLX_OFFLINE cf-server checks/tests/docs, migrated PostgreSQL tests, rustfmt, and backend-only diff inspection. Preserve all frontend/check changes; do not commit or push.

Final audit closure (2026-08-29): replace name-based option-value secrecy with a conservative typed value policy that redacts every string leaf in evaluated values, nested collections, package fields, module defaults, and evaluation messages before digest/index/persistence/API while preserving booleans, numbers, null, and explicit opaque/failed states; continue sanitizing source paths and URLs. Add pure and isolated PostgreSQL regressions using `GITHUB_PAT` and an arbitrary alias across storage, search, and API-shaped reads.

Move all flake-output module/declaration/default/consumer and lock input/follows/multiple-nixpkgs extraction into the single configuration-independent carrier. Remove configuration metadata snapshots and selection/fallback logic so each evaluation persists exactly one deterministic carrier result, including flakes with zero `nixosConfigurations`.

Restore compatibility for omitted deployment `request_id`: generate a non-durable per-call legacy identity and only reuse active pending work, so terminal same-target requests can intentionally redeploy. Keep explicit UI request IDs as the durable AC #21 retry contract and document the API distinction.

Extend migration 0235 with a durable explicit deployment-intent reservation keyed by system/request ID and immutable target/action. Reserve before policy conversion, carry conversion-persisted/deploy-failed/queued partial state across retries, bind the eventual deployment ID, and add an isolated concurrent reservation test proving conflicting intent loses before conversion. Run backend formatting, SQLX_OFFLINE checks/tests/docs, isolated migrated PostgreSQL tests, relevant real-Nix expression tests, and confirm frontend/check files are preserved.

Backend safe-value/replay/carrier correction (2026-08-29): replace blanket string and winner-note/error redaction with one deterministic recursive policy that preserves ordinary strings, package metadata, module defaults, and diagnostics while redacting sensitive option paths and attribute keys plus credential URLs, authorization/JWT/provider-prefix tokens, and high-entropy token-like values before digest/index/persistence/API. Add pure and PostgreSQL regressions for distinct safe/package values and absence from JSON/search/API. For omitted request_id, derive a stable system/commit/action identity and reuse it across pending and terminal states for a documented durable replay window of at least 24 hours; permit a new deployment after the boundary, while explicit request IDs remain durable and unambiguous. Make the independent flake carrier persist declared outputs and inputs without nixpkgs lib, choose cfg.pkgs.lib when configurations exist or an input lib otherwise for module evaluation, and emit explicit module-evaluation unavailable/error details instead of failing the complete carrier. Run focused cf-server tests, SQLX_OFFLINE check/tests/docs, migration-backed PostgreSQL tests, real-Nix expression tests where available, rustfmt and backend-only diff checks. Do not edit frontend/check files or commit/push.

Frontend independent-audit remediation (2026-08-29): restrict writes to `packages/web-ui` and preserve server, migration, and browser-check changes. Mirror the current server snapshot DTOs exactly, including optional removed options, typed option diffs, summary metrics, first-parent resolution, pagination, reconciliation collapse flags, provenance status/priority/note, and configuration-independent module/input payloads; add exact server-JSON deserialization tests. Reset revision-scoped Config controls and synchronize async current revision and popstate state. Render all safe value/diff kinds and removed options without fabrication; distinguish transport authorization/non-disclosure from unavailable lifecycle and gate evaluation mutation by `can_mutate`. Replace page-derived summary/module counts with authoritative response metrics and resolve tracked provenance for any input with a real source revision. Make flake pane/revision URL state authoritative, initialize after async commits, replace stale URLs, render typed module/input metadata and server deltas/counts/warnings, prefill branch correctly, and remove the leaked interval. Complete tab/row/dialog/source-tray keyboard semantics, focus trap/restoration, aria-modal and practical page inerting; stop child control bubbling. Keep auto_latest submitting until the request result closes it, refresh visible policy state, and correct lifecycle styling. Remove stale phase/UI-complete comments. Verify full web-ui tests and the wasm32 check through `nix develop`; do not edit checks, server, migrations, commit, or push.

Final backend gate (2026-08-29): preserve all frontend and browser-check files. Change the recursive option-value redaction boundary so any sensitive option path or nested sensitive field replaces every scalar leaf, including null, Boolean, number, string, and nested collection values, before digest, search, persistence, or API serialization. Add pure model/redaction regressions plus an isolated PostgreSQL test that proves numeric and Boolean secrets are redacted in stored payloads, absent from search, and represented only by the redaction marker in API rows. Evaluate exported modules with module-system checking disabled through an internal `_module.check = false` module, without adding declarations for unmatched definitions; preserve the explicit module-evaluation error for genuinely unevaluable modules. Add a real local Nix flake fixture whose exported module declares one option and also sets an unmatched `services` option, and assert that the declaration is extracted without a fabricated services declaration. Run focused tests in the Nix development environment with `SQLX_OFFLINE=true`, the isolated PostgreSQL regression if available, rustfmt, backend diff checks, and verify frontend/check diffs remain untouched. Do not commit or push.

Frontend/check P1 remediation (2026-08-29): restrict writes to `packages/web-ui` and `checks/web-ui`; preserve backend and migration changes. Repair TASK-440 browser fixtures to deserialize against every required current DTO field and align assertions with deterministic accessible markup. Make Config requests clear stale results immediately while retaining sequence guards; distinguish unknown generation from a retained generation with no commit; render summary cards only from available authoritative `module_count` and `evaluation_duration_ms`; report drift unavailable because no authoritative drift field exists; do not derive snapshot-wide modules from a bounded option page. Permit provenance navigation only for `self` when the system supplies its registered flake identity; external inputs remain visibly untracked because the server contract has no registered external flake identity. Add bounded flake-output collection continuation and page merging, use authoritative managed fleet totals for rollout denominators, and replace the URL revision when an unavailable commit falls back. Add browser back/forward and 900x900 flake coverage. Verify full web-ui tests, wasm32 check, JavaScript syntax, manifest parity, and targeted authoritative browser check if practical. Do not edit backend/migrations, commit, or push.

Final independent review remediation (2026-08-29): fix credential-bearing URL redaction for arbitrary URI schemes; make explicit evaluation requeue transition persisted failed/corrupt snapshots to queued and permit corrupt snapshot replacement; preserve retained-generation derivations during accepted history rewrites; make commit revision optional for generation API requests; represent over-depth option trees explicitly rather than silently dropping them. On the frontend, persist and reuse a stable deployment request ID and original action across partial auto_latest retries; reject non-full-SHA URL revision state; preserve keyboard focus across flake pane/revision rerenders; use authoritative flake input totals; clear stale Config responses immediately when requests change; and keep the auto_latest modal focus trap active while submitting. Add focused regressions, rerun targeted checks, then rerun the authoritative web-ui check before reconciliation/finalization. Preserve TASK-433's uncommitted 0236 allocation and do not touch its worktree.

Final reconciliation and publication (2026-08-29): merged current `origin/dev` and `origin/TASK-433-policy-poam-workflows` into the TASK-440 branch, preserved both snapshot exploration and POA&M contracts, and renumbered the TASK-440 migration to `0237_evaluation_and_flake_output_snapshots.sql` because TASK-433 owns 0233-0235 and its active worktree reserves 0236. Opened stacked draft MR !323 against the TASK-433 branch while !318 remains open. After !318 merges, retarget !323 to `dev`, reconcile any new integration commits, and rerun required checks before merge.

MR !323 P1 remediation plan (2026-08-29): (1) Refactor selected snapshot queries so commit and generation baseline candidates must pass the same schema, reference-count, content-existence, payload-shape, and decode integrity rules as current snapshots before comparison is enabled; add defense-in-depth isolation for a baseline that becomes invalid between selection and page loading. (2) Add a dedicated bounded, deterministic flake-module declaration page contract with authoritative totals and a content-digest continuation token; make module summary responses stop implying nested declaration completeness; load declaration pages explicitly in the flake Modules UI without N+1 queries. (3) Add a database-only selected-evaluation summary contract that aggregates all module/source rows from persisted provenance, joins selected-revision derivation and completion metadata, and returns exact evaluated/running store-path drift state. Restore the Config side hierarchy with authoritative module rows, richer evaluation facts, lifecycle/error/empty states, and truthful drift states; document unsupported historical/fingerprint metrics instead of fabricating them. (4) Resolve self and external provenance to server-issued registered-flake IDs only after exact repository identity matching and the existing caller visibility predicate; keep untracked, deleted, stale, ambiguous, and hidden targets non-navigable. (5) Add migration-backed SQLx regressions for invalid baselines, >100 declaration paging, module aggregation, provenance visibility, and thousands-option practicality; enroll focused tests in the existing server-regressions Nix check. Extend existing mocked browser workflows for UI state/design coverage and add the strongest established live server/database integration coverage without a bespoke framework. (6) Audit wide/narrow layout and design hierarchy, update architecture/API documentation and TASK-440 evidence, run focused checks before full web-ui and `nix flake check --keep-going -L`, then push without changing the stacked MR target. AC3/9/15/23/24 remain unchecked until behavior-level evidence supports each one.

Comparison baseline failure-isolation slice (2026-08-29): modify only cf-server snapshot query/test code and the focused server-regressions check. Apply one usability contract to commit and retained-generation baselines: lifecycle available, supported snapshot/content schema, exact reference count, required payload shape, and successful option decoding. Generation mode skips unusable retained predecessors and chooses the nearest earlier usable generation. Revalidate the selected baseline before page counts/rows so corruption cannot expose before/diff data or make the valid selected snapshot unavailable. Add ignored migration-backed SQLx regressions for corrupt, incompatible-schema, and missing-content baselines in both modes, plus valid comparison behavior; enroll only that focused regression in checks/server-regressions. Run focused rustfmt, SQLX_OFFLINE compile/tests, the PostgreSQL-backed regression through the Nix environment when available, and diff checks. Do not edit migration 0237 or frontend files.

Exported-module declaration pagination slice (2026-08-29): preserve the current uncommitted comparison-baseline isolation changes. Add a database-only GET endpoint for one exported module at one exact full revision. The query will select one persisted JSONB snapshot, apply deterministic declaration ordering, clamp limit to 100, return authoritative total/offset/applied limit and the flake content digest as a snapshot token, and reject continuation tokens after snapshot replacement. Keep the top-level output endpoint summary-compatible but clear nested declaration arrays and mark them incomplete so it no longer implies completeness. Reuse the existing flake visibility/non-disclosure policy and expose explicit unavailable/failed lifecycle responses without read-side mutation. Add server DTO/query/handler tests, including a migration-backed >100-row paging/token/auth/no-mutation regression, and enroll that focused regression in `checks/server-regressions/default.nix`. Mirror DTO/client contracts in web-ui and give each expanded module independent first-page/load-more loading, error, stale-response, and merge validation state with an accessible button. Extend pure frontend tests and the authoritative browser fixture to prove a row beyond 100 is reachable. Update API/architecture documentation, format touched code, run focused Rust tests and Nix server regressions, and do not commit or push.

Selected-revision Config parity slice (2026-08-30): preserve the uncommitted comparison-baseline and exported-module declaration pagination work. Add a database-only selected-evaluation summary endpoint that reuses commit/generation selection, aggregates every persisted option definition by exact source input/revision/path with defined/won counts, and reads selected snapshot/derivation plus latest system-state metadata for completion time, duration, option total, exact selected/running store paths, profile-match, closure package count, and exact store-identity drift. Resolve self and external tracked provenance server-side only: self uses the authorized system's active registered flake/full selected revision; external inputs require an exact selected flake-output input-name/source URL/full revision match to exactly one active registered flake and active commit, then the established active-system environment visibility predicate. Do not expose deleted, archived/stale, ambiguous, hidden, or unmatched identities. Mirror DTO/client contracts, fetch summary independently with stale-response protection, restore actual module rows and source-tray navigation, enrich Evaluation and Drift lifecycle/loading/error/empty/unavailable states, and retain the established wide/narrow hierarchy. Document that host delta, closure bytes, and seven-day fingerprint/history have no authoritative persisted source. Extend deterministic browser fixtures and focused DTO/pure/browser assertions. Add one migration-backed query regression covering aggregation, metadata/drift, and self/tracked/untracked/deleted/hidden/stale/ambiguous authorization; add endpoint validation/auth tests; enroll the focused DB test in server-regressions. Run focused rustfmt, SQLX_OFFLINE backend tests/check, web tests/wasm check, JS syntax/manifest checks, focused browser and server-regression checks where available, plus diff/documentation inspection. Do not alter migration 0237, commit, push, or change the MR target.

Independent remediation review adjustment (2026-08-30): confirmed additional P1 defects in the first implementation pass. Replace per-request full-corpus Rust baseline decoding with a bounded database integrity contract that does not transfer every baseline option for each page while retaining malformed-content isolation. Prevent selected-evaluation summaries from aggregating or exposing data unless the selected snapshot is usable. Redact any registered repository URL before DTO serialization, require self provenance revision to equal the selected full revision, and degrade malformed declaration rows to explicit unavailable lifecycle instead of HTTP 500. On the frontend, prevent provenance actions until the selected summary is available, render distinct summary loading/transport/lifecycle states in Modules, Evaluation, and Drift, and assert exact tracked revision navigation with consistent fixtures. Add focused regressions for each defect before broader verification.

Confirmed backend P1 remediation (2026-08-30): preserve all current work and modify only cf-server backend plus focused server-regressions enrollment; do not edit migrations, frontend, browser files, commits, pushes, or MR stacking. Replace full-corpus Rust baseline transfer/decoding and per-generation repeated checks with one server-side snapshot integrity contract and one-query generation fallback, while retaining bounded typed page decoding and race isolation. Gate selected summaries so any non-available or unusable selected snapshot returns lifecycle-only data. Sanitize registered repo_url only when constructing TrackedFlakeIdentity, require self provenance source_revision to equal the selected full revision, and downgrade malformed declaration rows to unavailable/corrupt lifecycle. Add migration-backed regressions for each behavior, multi-thousand bounded practicality, and practical authenticated handler authorization coverage; enroll only focused database regressions in server-regressions. Run rustfmt, SQLX_OFFLINE tests/check, server-regressions, and git diff checks without committing or pushing.

Backend remediation slice (2026-08-30): restrict writes to the six user-authorized files. Make SelectedEvaluationSummary scalar with authoritative exact module_source_total; add a bounded deterministic evaluation-module-sources endpoint whose grouped SQL preserves totals on empty pages; extract one private set-based tracked-provenance resolver and use it once for bounded module rows and once for all selected/baseline option definitions with their respective revision contexts. Keep response-only tracked identities out of persistence/redaction, sanitize repository URLs, and preserve active/visible/exact/unambiguous matching. Change snapshot module_count writes to exact distinct (source_input, source_revision, source_path), add additive migration 0238 to backfill existing snapshots without indexes, extend auth/validation and migration-backed large paging/provenance/read-only/query-count regressions, enroll focused ignored tests, then run targeted rustfmt, SQLX_OFFLINE checks/tests, migration-backed regressions where practical, and diff/documentation inspection.

Combined remediation review adjustment (2026-08-30): before browser/documentation work, correct module-source consistency by deriving `module_count` from the final redacted and bounded payload and adding a selected-snapshot continuation token so replacement cannot silently mix module pages. Keep query responses bounded and preserve exact tuple ordering/totals. Unify Config selected-revision refresh behavior so queue completion and polling refresh options, summary, and module page zero together; scope queue completions to the selected revision; clear stale source trays on scope changes; and provide accessible transport retries. Then update deterministic browser routes/assertions for the scalar summary and module-source endpoint, add pagination/continuation/error/race/provenance coverage, update API/architecture documentation, and rerun focused server, frontend, browser, and repository checks.

Focused frontend remediation (2026-08-30): restrict writes to `packages/web-ui/src/api/models.rs`, `packages/web-ui/src/api/client.rs`, `packages/web-ui/src/views/system_detail.rs`, and `packages/web-ui/assets/app.css`; preserve concurrent backend/browser changes. Mirror the evaluation module-source snapshot token and send it on continuation, rejecting token changes before merge. Replace Config options/summary/module-page-zero reload nonces with one exact selected-scope refresh generation and one queued/running polling timer. Scope queue completion to revision/mode/generation/refresh generation; clear source tray and expanded option on selected scope changes. Add keyboard-native transport retries and specific module continuation accessible names. Render baseline definition priority/winner notes like selected definitions, add focused pure DTO/scope/merge tests, then run targeted rustfmt, focused web-ui tests, wasm check if practical, and diff checks. Do not commit or push.

Focused backend pagination closure (2026-08-30): modify only migration 0238, cf-server evaluation snapshot model/query/system handler/router files, and focused server-regressions enrollment. Compute `module_count` only after evaluator options are redacted and individually bounded, with a regression proving oversized payloads that clear definitions persist zero module sources. Add a monotonic `snapshot_version` to evaluation snapshots and increment it on scoped snapshot replacement paths. Return that version as an opaque continuation token for evaluation module-source pages; require and validate it after offset zero; return typed `snapshot_changed` HTTP 409 on mismatch. Read integrity, total, rows, token, and tracked provenance within one read-only repeatable-read PostgreSQL transaction so a page cannot mix replacement versions. Add handler validation tests and an isolated migration-backed replacement/provenance regression. Correct tuple-count and complete ordering rustdoc. Run targeted rustfmt, SQLX_OFFLINE focused tests/checks, the selected PostgreSQL regression if practical, and diff checks. Do not address offsets above 100100 or migration batching, and do not commit or push.

Authoritative browser module-source fixture slice (2026-08-30): modify only `checks/web-ui`. Align the Config summary fixture with the scalar `module_source_total` DTO and add a deterministic routed `evaluation-module-sources` fixture with bounded query validation, lifecycle/transport/error controls, stable snapshot tokens, more than 40 ordered sources, tracked and untracked identities, exact-tuple duplication coverage, delayed requests, and replacement conflicts. Extend the existing mocked TASK-440 Config workflows to verify independent initial loads, 40-of-N continuation, row-preserving continuation failure and same-offset retry, initial unavailable/transport retry, in-flight revision isolation, direct option provenance independent of module-page loading, and practical 900x900 keyboard access. Keep mocked-versus-live labels explicit; run Node syntax, manifest parity, and focused authoritative browser workflows if the environment permits.

AC24 literal visual-parity pass (2026-08-30): treat `eb5a18513623890e9dac1e8a74565078243288a8` as authoritative and independently compare its SystemDetail, FlakeExplorer, FlakesView, EnvironmentsView, and styles against current Rust/Dioxus production surfaces. Inventory every missing, reordered, collapsed, metadata, interaction, and visual-hierarchy difference. Restore at minimum the Flake Systems All/Unmanaged/Undeclared segmented filter and lifecycle; Config Modules/Evaluation/Drift structure and authoritative rows; expanded module source/path hierarchy; reference Inputs columns; and geometry-sensitive Config composition. Determine authoritative persisted sources for host delta, closure size, agent fingerprint, and seven-day drift; implement only real backend/evaluation/agent data and never fabricate values. Add discriminating browser assertions and deterministic screenshot evidence at 1920x1080 dark/light and 900x900 narrow. Remove documented intentional deviations only after production parity exists. Then run focused frontend/WASM/browser/web-ui checks, backend/server regressions when contracts change, full flake checks, and a fresh independent review. Keep AC24 unchecked, TASK-440 In Progress, and MR !323 stacked/unmerged until literal parity and post-!318 reconciliation are complete.

AC24 authoritative backend contracts slice (2026-08-30): preserve frontend, browser, docs, protocol compatibility, and applied migrations 0237/0238. Extend the selected evaluation summary with a deterministic same-commit content-addressed host-overlay count, optional actual local Nix closure bytes, typed exact selected-versus-latest-agent store fingerprint, and typed seven-day exact store-path drift that requires continuous persisted observation coverage and fails closed on gaps. Add migration 0239 only for optional closure bytes unless query-plan evidence requires another index. Extend flake reconciliation with paging-independent mismatch totals, an optional server-side mismatch filter, and visible environment metadata without weakening non-disclosure. Extend the configuration-independent carrier with optional module attribute-position provenance mapped to self/input roots and direct-root descendant counts, preserve old JSON compatibility, and add input addition/removal deltas. Add focused pure, real-Nix where practical, and isolated PostgreSQL regressions plus focused server-regressions enrollment. Verify targeted rustfmt, SQLX_OFFLINE checks/tests, real-Nix tests, migration-backed regressions, and the server-regressions Nix check. Do not commit or push.

Config summary metrics correction (2026-08-30): restrict writes to cf-server, `checks/server-regressions`, and additive migration 0239. Preserve existing frontend/docs/applied migrations and unrelated TASK-440 work. Keep host delta database-only and deterministic across usable same-commit configuration snapshots, persist complete local recursive Nix closure bytes on derivations, return typed exact selected-versus-latest-agent store identity and fail-closed seven-day persisted observation status, and add the required covering observation index in 0239. Add focused pure and migration-backed regressions, then run rustfmt, SQLX_OFFLINE cf-server checks/tests, and the server-regressions Nix check. Do not commit or push.

Backend flake-contract closure (2026-08-30): preserve the current uncommitted Config metric work and all frontend/migration changes. Restrict writes to cf-server and checks/server-regressions. Complete the typed system-filter/reconciliation contract and tests; add backward-compatible optional typed exported-module source fields with normalized pre-persistence module paths from `unsafeGetAttrPos`; expose an authoritative `transitive_descendant_count` for direct lock roots from the complete graph while retaining the currently consumed legacy field; verify exact input add/remove deltas. Add pure, migration-backed, and real-Nix extraction regressions, enroll focused tests in server-regressions, then run rustfmt, SQLX_OFFLINE checks/tests, the real-Nix test, and server-regressions. Do not edit or commit frontend, docs, or migrations 0237-0239.

AC24 production frontend parity slice (2026-08-30): preserve all current backend/browser/frontend work and write only under `packages/web-ui`. Use the four files at design commit `eb5a18513623890e9dac1e8a74565078243288a8` as the direct target. Replace Config's fixed option limit with DOM-measured side-column/table geometry (24 initial, 10..80 clamp, one-row hysteresis, offset reset, resize plus content-triggered delayed remeasurement), while retaining request sequence/scope race guards. Align Config revision/search/pager/table density, Modules 300px source list, exact Evaluation/Drift hierarchy, truthful lifecycle badges, typed seven-day drift, and 720px provenance tray. Correct flake declared/direct tab counts and stale dot, revision-reset server-backed Systems filtering and global counts, revision delta tooltips, exact Systems/Modules/Inputs column hierarchy and direct-input rows, and pane-specific 900px responsive behavior without inner scrollers. Update only web-ui DTO/client contracts and pure tests as required. Run scoped rustfmt, full web-ui tests, wasm32 check, and web-ui diff checks; report any remaining parity gaps without claiming full parity.

AC24 browser closure slice (2026-08-30): modify only `checks/web-ui`. Extend the existing deterministic TASK-440 fixtures with all current Config summary metrics, typed seven-day states, authoritative flake reconciliation totals/environment metadata, module source paths, direct-input descendant counts, and typed input deltas. Add exact semantic and geometry helpers plus dedicated end-state workflows for Config wide expanded at 1920x1080, Config narrow at 900x900, and Flake Systems/expanded Modules/Inputs at both viewports. Enroll every workflow in `coverage-manifest.json` with honest mocked-data and critical metadata and in the Nix critical list. Retain automatic dark/light captures without approving baselines. Verify Node syntax, manifest parity, and focused authoritative workflows through `CF_UI_TEST_STEPS` and `nix build --impure 'path:.#checks.x86_64-linux.web-ui'`; do not edit production/docs or commit/push.

Backend latest-review remediation (2026-08-31): preserve all current changes and restrict writes to `packages/default/crates/cf-server`, migration 0239, and `checks/server-regressions/default.nix`. Materialize deterministic same-commit host delta on `evaluation_snapshots` and recompute it inside every snapshot persistence transaction; make summary scalar. Use the fixed four-hour offline coverage boundary. Replace Rust fleet reconciliation with one visibility-aware set-based SQL page that computes totals, filters, stable ordering, and authoritative global warning counts. Treat `unsafeGetAttrPos` only as export binding location with longest-unique-root attribution; correct direct versus transitive lock descendants and document the compatibility alias. Remove migration 0239's duplicate system-state index. Stream closure `path-info` output under a byte limit with kill-on-drop, complete-success JSON validation, and duplicate-size conflict rejection. Return revision-global deltas only on offset zero. Add focused pure, real-Nix, large PostgreSQL/query-count/plan regressions, enroll them in server-regressions, run rustfmt, SQLX_OFFLINE checks/tests, real-Nix focused tests, and the requested Nix server-regressions build, then inspect/stage only scoped files, commit, and push.

Frontend Config/Flake parity closure slice (2026-08-31): preserve backend HEAD 4e09d60a and all existing checks/web-ui changes; write only packages/web-ui. Keep the prior available Config page mounted during debounced/background requests, guard raw and debounced query identity, dim stale rows, and expose Querying status/clear behavior. Compute option page size from natural Modules/Evaluation/Drift card rectangles and actual inter-card gaps with the established formula, clamp, hysteresis, and offset reset. Restore exact eb5a185 Config/Flake dimensions and source-tray hierarchy while rendering only real provenance metadata and explicitly unavailable source content; label available evaluation data cached. Mirror current backend flake global counts, systems continuation, and transitive descendant fields; make continuation active-pane-local, deduplicate page/global delta merges, preserve full SHA accessibility/history identity, render missing export bindings unavailable and follows strings without JSON quotes. Add focused DTO and pure tests, then run targeted rustfmt, full web-ui tests, wasm32 check, git diff --check, and verify backend/browser/docs diffs remain unchanged. Do not commit or push.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Preflight 2026-08-28: dedicated worktree `/home/mcamp/code/crystal-forge/TASK-440-system-config-flake-parity`, branch `TASK-440-system-config-flake-parity`, based exactly on `origin/dev` at `a9747294b153eddcb17906336fe3ec31a474c217`. User explicitly directed that the unrelated dirty local `main` worktree be ignored and that this task branch from `origin/dev`; local `dev` was not used as the base. Lock created at `backlog/.locks/TASK-440.lock`.

Parallel TASK-433 coordination baseline: open MR !318 at https://gitlab.com/crystal-forge/crystal-forge/-/merge_requests/318, current fetched head `fd7548ad0b983e2412a5d1fa2b84ac397280daaa`. Its current additive migrations are `0233_composite_policy_assessments.sql` and `0234_poam_workflows.sql`. TASK-440 must re-inspect the remote branch before allocating migrations and rebase/update onto TASK-433 after merge before TASK-440 merges.

Research inventory completed for backend evaluation/snapshot ownership, frontend navigation/surfaces, design delta, and authoritative browser coverage. Key findings: current Config is static; flake output panes/snapshots do not exist; normal timeline reads are DB-only but commit diff GET has side effects; first-parent identity and generation snapshot retention are absent; current redaction is too narrow; ordinary flake counts mix declared/managed/deployed relationships; `/api/v1/commits/:commit_id/re-evaluate` needs authorization review; TASK-433 overlaps shared DTOs/routes/System Detail/Compliance/notifications/CSS/tests.

Initial Phase 1 slice implemented in the TASK-440 worktree: flake focus/selection and row keys now use exact full SHA rather than displayed prefixes; added a regression with two distinct full SHAs sharing the same seven-character display prefix; diff modal now uses an explicit above-drawer overlay class instead of an inline z-index below the tray.

Verification: `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml displayed_sha_prefix_never_aliases_full_commit_identity` passed (1 passed, 192 filtered out). `git diff --check` passed. Full web-ui `cargo fmt --check` is currently blocked by pre-existing formatting drift in TASK-433-origin files on `origin/dev` (including policy editor, compliance, policies, and adapter files); no unrelated formatting was applied.

Phase 2 security foundation started without allocating migrations: added documented `security::snapshot_redaction` boundary for recursive JSON redaction and diagnostic text sanitization before future snapshot persistence, indexing, digesting, logging, or API serialization. It redacts sensitive nested fields, authorization headers, common secret assignments, URL userinfo, query strings, and fragments while preserving non-sensitive diagnostic text.

Verification: `nix develop -c env SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server snapshot_redaction` passed (3 passed, 1564 filtered out; remaining targets had zero matching tests). The same command without `SQLX_OFFLINE=true` failed at compile time because no PostgreSQL server was running; this was an environment/SQLx macro issue, not a test failure. The new redaction source file was formatted with repository rustfmt.

Frontend Phase 1 and isolated cross-surface slice implemented in `packages/web-ui` without editing server, migrations, SQLx metadata, or browser `integration-test.js`. Added typed URL query contracts for System Detail tab/Config revision/Deploy generation and flake drawer pane/full revision/environment return context, including URL encoding, stale-context clearing, popstate synchronization, exact full-SHA retention, and focused round-trip tests. System Detail now removes duplicate header Deploy/Rollback actions, History rollback opens Deploy with the exact generation selected, selectors read `New commit` and `Previous generation`, and Config exposes URL-backed generation/commit revision controls with explicit unavailable linked-revision state. Environment flake chips open the exact flake context and restore the originating panel on tray close. Notification parsing now accepts only an exact UUID system path plus exact `tab=deploy` and rejects prefix aliases. The duplicate inner Compliance Edit was removed while the authorized `BundleHeader` edit remains. Flake tray/diff modal now use explicit tray < modal < toast ordering, initial focus, Tab/Shift+Tab trapping, modal-first Escape handling, and opener focus restoration for table/card/file controls. Verification passed: `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml` (198 passed, 1 ignored); `nix develop -c cargo check --manifest-path packages/web-ui/Cargo.toml --target wasm32-unknown-unknown`; `git diff --check`. Existing repository warnings remain. Remaining frontend gaps for later phases: real evaluation/flake-output snapshot APIs and pane content, full Config option explorer/search/diffs/lifecycle states, Flake Systems/Modules/Inputs data, Config-to-flake and Flake-to-Config domain rows, auto_latest conversion workflow, and authoritative browser coverage/screenshots. Concurrent server/migration changes visible in the worktree were preserved and not modified by this frontend slice.

Backend snapshot slice implemented in `/home/mcamp/code/crystal-forge/TASK-440-system-config-flake-parity` without commits or pushes. Migration allocation decision: fetched `origin/TASK-433-policy-poam-workflows` at `fd7548ad0b983e2412a5d1fa2b84ac397280daaa`; TASK-433 still owns 0233/0234 and `origin/dev` ends at 0232, so TASK-440 uses additive `0235_evaluation_and_flake_output_snapshots.sql`. No TASK-433 migration was edited.

Implemented: tagged safe option values/errors/provenance and lifecycle DTOs; recursive redaction before digest/search/persistence; per-option SHA-256 content deduplication; retained-generation and flake-output snapshot schema; options-tree and flake output extraction in the existing bulk nix-eval-jobs metadata path; atomic snapshot persistence in evaluation finalization; database-only environment-scoped options and flake output endpoints; bounded search/filter/count/pagination and baseline diff query; explicit unavailable/queued/running/failed/available states; full-SHA validation; idempotent authorized queue/reuse action; authorization added to legacy re-evaluate; managed/declared/undeclared reconciliation and authoritative managed counts. No cf-protocol or cf-builder changes were required, preserving deployed compatibility.

Verification: `nix develop -c env SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server --lib snapshot` passed (11 passed, 4 ignored, 1558 filtered out). `nix develop -c env SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server --lib test_build_expression_no_policies` passed (1 passed, 1571 filtered out). `git diff --check` passed. A broader `cargo test ... snapshot --no-fail-fast` compiled and passed the matching library tests but timed out after 120 seconds while Cargo continued unrelated integration binaries, so it is not recorded as a passing command. PostgreSQL was unavailable (`pg_isready`: no response), so migration execution, database query behavior, and SQLx metadata refresh were not run.

Remaining backend gaps: authoritative Git first-parent identity is represented but not yet extracted/populated, so comparison correctly reports unavailable instead of inferring or fabricating zero; generation-retention references are modeled but not yet linked from retained system-state ingestion; flake modules currently include exported names but not declaration/default/consumer detail; inputs include source/revision/time but not full follows/transitive/channel analysis; option priority metadata does not expose discarded definitions, so winning/overridden flags are not fabricated; removed-option diff rows and package-element-specific diff summaries remain; no live-PostgreSQL migration/dedup/retention/API authorization tests were possible. TASK-440 still requires reconciliation onto merged TASK-433 before merge eligibility.

Frontend snapshot-contract slice verification (2026-08-28): completed `packages/web-ui` consumption of the concurrent evaluated-options and flake-output APIs. Config now uses URL-selected generation/commit context, explicit snapshot lifecycle and queue action, 220 ms debounced bounded server queries with stale-response rejection, revision-global counts, filters, pagination, typed value/diff/provenance rows, source details, and real snapshot summary cards. The flake drawer now binds Commits/Systems/Modules/Inputs panes to the selected full SHA, renders lifecycle/no-first-parent states and output deltas, reconciles managed/declared systems, and preserves exact Config/registration navigation. The Systems registration form consumes the flake/configuration/hostname/branch query prefill. No browser JavaScript or files under `checks/` were edited. Verification passed: `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml` (205 passed, 1 ignored, 0 failed); `nix develop -c cargo check --manifest-path packages/web-ui/Cargo.toml --target wasm32-unknown-unknown`; `git diff --check -- packages/web-ui`. Existing repository warnings remain. Contract-limited behavior is explicit: the current flake snapshot emits exported module names but not declaration/default/consumer details, and input rows lack follows/transitive/channel metadata; the UI labels those facts as not emitted. Config provenance navigation is enabled only when source input/revision metadata exists, and missing first-parent data reports no comparison rather than a zero delta. Acceptance criteria remain unchecked because the repository finalization rules require authoritative browser/manual evidence, and browser integration work was explicitly excluded from this slice. The task remains In Progress; no commit, push, MR, screenshot, or status transition was performed.

Backend gap-closure verification (2026-08-28): added authoritative Git first-parent extraction from `%P` and persistence across Git sync/insertion paths; snapshot comparisons now resolve only persisted first-parent identity. Linked retained-generation snapshots from agent system-state/heartbeat ingestion and backfilled observations during snapshot finalization. Expanded options diffing to include removed paths and typed package/list/attribute-set deltas. Expanded cached flake metadata for module declarations/defaults/consumers and direct/resolved/follows/transitive input, channel, age, bump, stale, and multiple-nixpkgs-revision states. Added PostgreSQL coverage for root and unresolved-parent no-comparison responses, full-SHA prefix collisions, deduplication, removed-option pagination/counts, side-effect-free reads, retained-generation metadata after store-path loss, and commit deletion protection. Migration 0235 succeeded on both the current migration chain and an extracted TASK-433 0233/0234 chain. Final targeted verification passed: `nix develop -c env SQLX_OFFLINE=true cargo check --manifest-path packages/default/Cargo.toml -p cf-server --tests`; `nix develop -c env DATABASE_URL=postgresql://mcamp@127.0.0.1:3042/crystal_forge CRYSTAL_FORGE_TEST_DATABASE_URL=postgresql://mcamp@127.0.0.1:3042/crystal_forge SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server snapshots_deduplicate_and_full_sha_prefixes_do_not_alias -- --ignored`; and `nix develop -c env SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server generated_evaluation_expression_parses_when_nix_is_available`. Existing repository warnings remain. No commit or push was made. Remaining validation: execute the expanded extractor against a representative real flake, complete authoritative browser coverage/screenshots, run broader task-level builds/checks, and reconcile TASK-433 before merge.

AC #21 slice implemented without a new migration and without touching `checks/web-ui/tests/integration-test.js`. The deploy API now accepts typed `deploy`, `continue_auto_latest`, and `convert_to_manual` actions and returns independent persisted-policy, conversion, deployment, and deployment-ID state. Conversion commits before deployment so a target failure returns persisted manual plus failed deployment; conversion errors return before any deployment call. Pending deployment requests serialize on the system row and reuse the active pending deployment ID for retry/concurrency idempotency. Conversion-plus-failed-deploy audit state and successful deploy action/policy/outcome are recorded through the existing system mutation audit helper.

Web UI: added an isolated `AutoLatestDeployPrompt` and reducer with Cancel, Continue on auto_latest, and Convert to manual and deploy. System Detail sends the selected typed action, treats typed failed deployment as failure even on a parseable non-2xx response, and immediately applies the returned persisted policy locally so a converted system retries as manual without reload. Existing CSRF request machinery remains in use.

Verification passed: `nix develop -c env SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server --lib handlers::api::systems::tests` (39 passed, 2 ignored); isolated migrated DB command `... cargo test ... manual_conversion -- --ignored` (2 passed) proved failed conversion queues zero, conversion survives deployment failure, and retry reuses one pending deployment ID; `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml auto_latest_deploy` (3 passed); touched-file `rustfmt --check`; `git diff --check`; and `git diff --exit-code -- checks/web-ui/tests/integration-test.js`. Repository warnings remain. The worktree-local database was stopped after DB verification. SQLx metadata was not refreshed because all new queries use runtime `sqlx::query`/`query_scalar` and no query macro shape changed.

Remaining gap for this slice: no browser-level interaction/screenshot check was added because the user explicitly prohibited changes to `integration-test.js`. The focused Rust UI tests cover reducer transitions and required action labels, while rendered modal behavior remains for the task's later authoritative browser phase. Other TASK-440 files remain concurrently modified by parallel agents and were preserved.

Authoritative browser follow-up (2026-08-28): fixed the flake module keyboard-handler ownership error; preserved Config results during refetch and added request-parameter stale-response validation; made flake pane state URL-authoritative and restored the commit grid with explicit `display:grid`; changed environment-to-flake and return navigation to typed Dioxus routes; corrected the generated evaluator expression's invalid `builtins.min` calls; and hardened TASK-440 fixtures to release all duplicate held search requests and wait for the replacement response before asserting stale-response behavior. Verification passed: `nix develop -c cargo check --manifest-path packages/web-ui/Cargo.toml --target wasm32-unknown-unknown`, `nix develop -c env SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server generated_evaluation_expression_parses_when_nix_is_available`, `nix develop -c node --check checks/web-ui/tests/integration-test.js`, and `git diff --check`. Authoritative browser completion remains blocked: after the evaluator fix, the web-ui VM's background evaluator advances into repeated real Nix system builds, emits `/nix/store/.links/... has maximum number of links`, and exceeds 15-30 minute command timeouts before producing a browser summary. Earlier pre-fix runs reached the workflows and identified the corrected interaction races. No commit, push, MR, task status change, or unrelated cleanup was performed.

Backend remediation verification update (2026-08-28): added strict membership filtering for flake-output declared systems/module consumers, registry/timeline build aggregation, and non-admin cached policy totals. Added direct payload-filter and literal LIKE-escape tests plus membership-filtered registry/timeline DB assertions. Verification passed: SQLX_OFFLINE cf-server --tests check; changed-backend-file rustfmt --check; git diff --check; cf-server rustdoc build (existing warnings only); 4 snapshot query unit tests; 4 security redaction tests; 7 snapshot model tests; generated Nix parser test; shallow boundary-parent Git test; auto_latest explicit-manual-choice test; 3 isolated SQLx snapshot DB tests serially; and set-based registry/timeline isolated DB test for admin and membership-filtered paths. The verified DB was the worktree-local process-compose PostgreSQL on 127.0.0.1:3042. No frontend/browser files were edited, committed, or pushed by this backend pass.

Final backend follow-up verification (2026-08-29): reran the final migration state against the repository-owned PostgreSQL instance on 127.0.0.1:3042 with CREATEDB role `mcamp`. `snapshots_deduplicate_and_full_sha_prefixes_do_not_alias` passed 1/1 and proved migration 0235, content deduplication, full-SHA prefix isolation, root no-parent behavior, and missing-parent no-comparison behavior. `retained_generation_survives_store_metadata_loss_and_blocks_commit_deletion` passed 1/1 and proved retained generation attribution survives store metadata loss and prevents commit deletion. Final `SQLX_OFFLINE=true cargo check --manifest-path packages/default/Cargo.toml -p cf-server --tests`, `SQLX_OFFLINE=true cargo doc --manifest-path packages/default/Cargo.toml -p cf-server --no-deps`, and `git diff --check` passed. Existing repository compiler/rustdoc warnings remain; no new TASK-440 TODO/FIXME markers were found in the new snapshot files. Worktree status remains intentionally dirty with the full TASK-440 backend, frontend, and browser slices; no commit or push was made. The branch is one documentation-only AGENTS.md commit behind origin/dev. TASK-433 has not merged and dev still ends at migration 0232, so mandatory post-TASK-433 reconciliation remains pending. The authoritative web-ui VM check remains incomplete because its real Nix evaluator/build path exceeds local command timeouts and reports `/nix/store/.links/... has maximum number of links`; task stays In Progress and no acceptance criteria were checked.

Final eight-defect remediation verification (2026-08-29): implemented and verified the backend-only fixes in cf-server plus migration 0235 while preserving all existing `packages/web-ui` and `checks/web-ui` changes. Evaluation failures are redacted before logs/persistence/broadcast; redaction now covers credential-bearing ssh/git+https/scp URLs, bearer/token diagnostics, and nested lock metadata. Snapshot extraction no longer truncates persisted options/configurations/modules, classifies before bounded forcing, and records per-value failures or oversized snapshots without aborting finalization. Reciprocal generation attribution requires matching pending-deployment commit identity. Source reset archives retained commits so generation reads survive while active-revision authorization rejects the old lineage. Deployment requests require full 40/64-character hexadecimal identities, persist immutable request/commit identity, use durable explicit IDs and five-minute implicit retry windows, and create a new lifecycle row for distinct same-store requests. Policy conversion now commits independently before deployment queueing, so a later queue failure reports persisted manual policy and retries remain idempotent.

Verification for the final remediation passed: `nix develop -c env SQLX_OFFLINE=true cargo check --manifest-path packages/default/Cargo.toml -p cf-server --tests`; targeted changed-file `rustfmt --check`; `git diff --check`; generated Nix expression parsing; focused redaction/extraction/identity unit regressions; isolated migrated-PostgreSQL `retained_generation_survives_store_metadata_loss_and_blocks_commit_deletion`; and isolated migrated-PostgreSQL `manual_conversion` regressions (2 passed). A final `SQLX_OFFLINE=true cargo test ... -p cf-server --lib snapshot_` passed 11 tests with 4 explicitly ignored DB tests. An earlier broad `snapshot_` filter also ran unrelated `assignment_semantics` integration tests, which failed only because the local role lacked setup-database permission; narrowing to `--lib` produced the successful requested result. Existing repository warnings remain. The worktree-local PostgreSQL stack was stopped after verification; process-compose had been started with `--no-server`, so `db-only down` could not contact its control API, and the verified process-compose/PostgreSQL process group was terminated gracefully with SIGTERM. Port 3042 has no remaining listener. Documentation/diff inspection found no new TASK-440 TODO/FIXME markers. No commit, push, MR, frontend/check edit, acceptance-criteria check, or task-status transition was performed. TASK-440 remains In Progress because its broader browser/build verification and TASK-433 reconciliation are still pending.

Final backend closure (2026-08-29): completed the requested backend-only punch list without editing existing `packages/web-ui` or `checks/web-ui` changes. Secret path normalization now removes naming separators and case distinctions, covering camelCase/common aliases including `apiKey`, `privateKey`, `accessKey`, `passphrase`, `authToken`, `clientSecret`, and `signing_key` while preserving non-secret `publicKey`. Explicit deployment request identity now persists immutable action plus commit context; a UUID reused with another commit/action returns HTTP 409 with typed `deployment: conflict` and the prior deployment ID instead of reporting successful reuse. Conflict checks happen before policy conversion, action planning, and target availability. Omitted request IDs now define one durable legacy `(system, commit, action)` retry intent across delayed and terminal retries; a new explicit UUID is the documented intentional-redeployment boundary. Over-limit lists and attribute sets are emitted as explicit opaque `list_over_limit` / `attribute_set_over_limit` values, never incomplete prefixes. Flake output now has an independent reserved evaluator carrier that works with zero NixOS configurations; any successful configuration can enrich it, so a failed first configuration does not suppress flake metadata. Active system, registry, dashboard, timeline, evaluation-queue, latest/distance, and pending-evaluation commit queries exclude `source_archived` rows; registry/timeline tests cover an archived former snapshot head.

Verification passed in the repository Nix environment: generated Nix expression parse; focused camelCase secret-redaction test; explicit opaque collection extraction test; independent carrier/zero-configuration/later-success flake-output tests; durable implicit/explicit identity unit test; typed conflict wire-value compile/test coverage; fresh migrated PostgreSQL `snapshots_deduplicate_and_full_sha_prefixes_do_not_alias` (includes commit/action request-ID conflicts and terminal retry reuse); fresh migrated PostgreSQL `set_based_registry_and_timeline_queries_use_snapshot_order` (includes archived-head exclusion); `SQLX_OFFLINE=true cargo check --manifest-path packages/default/Cargo.toml -p cf-server --tests`; `SQLX_OFFLINE=true cargo doc --manifest-path packages/default/Cargo.toml -p cf-server --no-deps`; cf-server rustfmt check; and backend `git diff --check`. Existing repository warnings remain. No SQLx offline metadata refresh was needed because changed SQL uses runtime typed queries. No commit or push was made.

Final audit closure verification (2026-08-29): conservative typed redaction, the configuration-independent flake-output carrier, legacy omitted-request compatibility, and durable explicit request reservations are implemented in the dedicated TASK-440 worktree. Final Nix-devshell verification passed: `SQLX_OFFLINE=true cargo check --manifest-path packages/default/Cargo.toml -p cf-server --tests`; `SQLX_OFFLINE=true cargo doc --manifest-path packages/default/Cargo.toml -p cf-server --no-deps`; 7 `snapshot_redaction` tests; 8 `models::evaluation_snapshots::tests`; carrier-shape and generated-Nix parse tests; explicit/legacy identity unit test; isolated SQLx tests for arbitrary-string absence from storage/search/API, retained-generation survival, concurrent explicit reservation conflict before conversion, and terminal legacy redeployment/manual-conversion retry behavior. Scoped `rustfmt --edition 2024 --check` passed for all final-remediation implementation files, and `git diff --check` passed. The repository-wide server `cargo fmt --all -- --check` remains blocked only by pre-existing import ordering in `src/api/models.rs`; that file's TASK-440 change is documentation, so unrelated formatting was not rewritten. Rustdoc completed with the repository's existing unrelated broken-link warnings. Frontend and browser-check changes remained present and were not modified during this backend final-audit closure. No commit, push, MR, or task status transition was performed.

Backend safe-value/replay/carrier correction completed (2026-08-29), no commit or push. Replaced blanket string/error/winner-note masking with a documented deterministic policy: sensitive option paths and normalized secret-bearing nested keys redact values and remove key names; credential URLs, authorization/bearer text, JWTs, GitHub/GitLab/cloud/provider prefixes, and high-entropy token-like strings are removed; ordinary strings, package identity/store metadata, module defaults, evaluator detail, and winner notes remain distinct and searchable. Sanitization still occurs before digest/search/persistence/API. Added pure package/default/note/error/token regressions and migrated PostgreSQL proof that GITHUB_PAT/token/clientSecret keys plus nested values, credential URL material, bearer/JWT/cloud tokens are absent from payload/search/API while safe strings remain searchable.

Legacy omitted-request-ID deployment calls now derive stable SHA-256 identity from system/full commit/action. Documented and implemented a conservative 24-hour replay window across pending and terminal states; a 23-hour terminal retry reuses the deployment and a 25-hour call creates an intentional redeployment. Explicit request IDs remain durable and unambiguous. Migration 0235 now uses a non-unique time-ordered identity index so legacy identity can recur after the window while system-row locking serializes creation.

The configuration-independent flake carrier no longer throws without an input lib. It prefers the first available configuration's cfg.pkgs.lib/pkgs, falls back to an input lib/legacyPackages, and otherwise emits declared systems, exported module names, and resolved lock inputs with module_evaluation.available=false plus explicit error detail. Missing nixosConfigurations and missing flake.lock no longer abort the carrier. A real-Nix local path-input flake with no nixpkgs/lib proves declared module and input persistence.

Verification passed: focused snapshot_redaction 9/9; evaluation snapshot models 9/9; generated Nix parser; real-Nix no-lib carrier; carrier structure and stable legacy identity tests; SQLX_OFFLINE cf-server --tests check; migration chain through 0235 on repository-isolated PostgreSQL 127.0.0.1:3042; migrated DB secret storage/search/API test; migrated DB 23h retry/25h redeploy test; cargo sqlx prepare --check --workspace with no metadata changes (existing unused-query warning only); cf-server cargo doc --no-deps (existing rustdoc warnings only); rustfmt and git diff --check. Frontend/check files were not edited in this correction pass and remain preserved.

Frontend independent-audit remediation completed in the dedicated TASK-440 worktree. Writes were limited to `packages/web-ui`; existing server, migration, and `checks/web-ui` changes were preserved. Frontend DTOs now deserialize the exact current server evaluated-options and flake-output JSON, including optional removed options, typed diffs, lifecycle metrics, provenance status/priority/note, first-parent resolution/delta/pagination, collapse flags, typed modules/declarations/consumers, and resolved inputs/lock metadata; exact JSON contract tests were added. Config revision remounting resets search/filter/page/expanded/source state, async requests reject stale results, mutation actions are hidden without system mutation permission, protected-resource errors are non-disclosing, removed/failed/opaque/package/structured values and diffs render safely, summary/drift metrics are authoritative, and external tracked provenance retains its own input/revision. Flake pane/revision URL state is authoritative across async commit loading and popstate, stale revisions are replaced without adding history, output fetches reject stale responses, Commits shows the revision bar/server delta, Systems uses authoritative counts/collapse/pin data, Modules and Inputs render the complete typed carrier payload and warnings, and registration retains branch context. Removed the leaked commit interval and stale UI-complete/phase comments. Added tablist keyboard navigation, real row buttons, modal/source-tray focus traps and restoration, aria-modal semantics, child-event isolation, truthful auto_latest submitting state, and immediate persisted-policy display refresh. Verification passed: `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml` (207 passed, 1 ignored); `nix develop -c cargo check --manifest-path packages/web-ui/Cargo.toml --target wasm32-unknown-unknown`; `git diff --check -- packages/web-ui`. Existing repository warnings remain. No commit or push was made.

Final backend gate (2026-08-29): sensitive option-path context now replaces null, Boolean, number, and string scalar leaves with `[REDACTED]`, recursively across nested values and all `SafeOptionValue` shapes before digest/search/persistence/API. Added pure recursive/model tests and an isolated migrated-PostgreSQL regression proving numeric `8675309` and Boolean `true` secrets persist only as markers, return only as markers, and produce zero search matches. Exported-module declaration extraction now evaluates `[ module { _module.check = false; } ]`, filters internal `_module` declarations, and retains the existing explicit error fallback for genuinely failed module evaluation. Added a real local Nix flake fixture using the repository's locked nixpkgs input; its exported module declares `crystalForgeFixture.message` and sets unmatched `services.unmatchedForDeclarationExtraction.enable`, and extraction returns exactly the declared option without fabricating the services path. Verification passed: three focused `SQLX_OFFLINE=true` unit/real-Nix tests; isolated PostgreSQL test `numeric_and_boolean_secrets_are_redacted_in_storage_search_and_api`; `SQLX_OFFLINE=true cargo check -p cf-server --tests`; rustfmt on touched backend files; and whole-worktree `git diff --check`. Existing repository warnings remain. No migration/query-macro shape changed, so SQLx metadata did not require refresh. Existing frontend and `checks/web-ui` changes were preserved; no commit or push was made. TASK-440 remains In Progress because the overall task is not being finalized and no MR was requested.

Frontend/check P1 remediation completed in `/home/mcamp/code/crystal-forge/TASK-440-system-config-flake-parity` without backend or migration edits. Browser fixtures now include required evaluated-options generation/snapshot/summary/diff/provenance fields and the complete typed flake output payload, first-parent state, reconciliation fields, deltas, and pagination. Assertions now target current buttons/accessible markup and deterministic managed totals. Config clears rendered rows and marks summaries unavailable immediately for search/filter/page/revision requests while retaining controls and stale-response sequence protection. It distinguishes unknown generations from retained generations without commits. Modules/Evaluation use authoritative `module_count` and `evaluation_duration_ms`; snapshot-wide module paths are no longer inferred from the current options page. Drift is explicitly unavailable because the server response has no authoritative drift field. Provenance navigation is limited to `self` with the system's registered flake; external input names remain visibly disabled because no registered external-flake identity exists in the server DTO. Flake output requests now send bounded offset/limit, merge continuation pages, expose a continuation control, retain authoritative totals, use the managed fleet count for rollout denominators, and replace URL/navigation state when an unavailable commit falls back. Browser coverage adds Config and flake back/forward, unknown generation, collection continuation, registered-identity gating, and 900x900 flake assertions. Added pure regressions for page merging/totals and rollout denominators.

Verification 2026-08-29: `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml` passed (209 passed, 1 ignored); `nix develop -c cargo check --manifest-path packages/web-ui/Cargo.toml --target wasm32-unknown-unknown` passed; `node --check checks/web-ui/tests/integration-test.js` passed; `git diff --check -- packages/web-ui checks/web-ui` passed; index is clean. Targeted authoritative browser command `CF_UI_TEST_STEPS=12l-task440-config-lifecycle,12m-task440-config-explorer-keyboard-wide,12n-task440-config-narrow-keyboard,13j-task440-flake-states-panes-navigation nix build --impure .#checks.x86_64-linux.web-ui -L` was attempted twice. First it stopped before Playwright because Git flakes omitted existing untracked `auto_latest_deploy_prompt.rs`. After temporarily staging only that file, the frontend package compiled, but the check again stopped before Playwright because the Git flake omitted the existing untracked backend `models/evaluation_snapshots.rs`, `queries/evaluation_snapshots.rs`, and `security/snapshot_redaction.rs`. The frontend file was immediately unstaged; backend files were not staged or modified. No browser workflow executed, so no browser pass is claimed. Existing warnings remain. No commit or push was made.

Resume check on 2026-08-29: fetched remotes. TASK-440 is at `a9747294`, one documentation commit behind `origin/dev` (`90992d86`). MR !318 / TASK-433 remains open at `eb4097e7`, its pipeline passed, but GitLab reports conflicts with current `dev`; TASK-440 therefore cannot perform the required post-merge reconciliation yet. The intended TASK-440 diff is staged except for three final server test corrections, which passed `git diff --check` and will be staged before the authoritative Web UI check.

Final integrated verification (2026-08-29): committed merge repairs as `fa25b253`. `nix build .#checks.x86_64-linux.web-ui --no-link -L` completed successfully. All critical workflows passed, including TASK-440 workflows `12l`, `12m`, `12n`, `12o`, `13j`, `13k`, and `14d`, plus reconciled TASK-433 workflows `29g` and `29k`. The `ci_fast` harness reported permitted non-critical failures in unrelated legacy scenarios. The derivation produced dark/light screenshots, including 1920x1080 Config and flake explorer captures and a 900x900 Config capture. `nix flake check --keep-going -L` completed successfully for x86_64-linux with all checks passed; incompatible aarch64-linux and Darwin systems were omitted by Nix. Existing compiler warnings remain. `git diff --check origin/TASK-433-policy-poam-workflows...HEAD` passed and the worktree was clean before publication. Branch pushed and stacked draft MR !323 opened with screenshots: https://gitlab.com/crystal-forge/crystal-forge/-/merge_requests/323.

P1 remediation reopened 2026-08-29 for MR !323. The prior acceptance state and mocked browser evidence are not treated as proof. AC3, AC9, AC15, AC23, and AC24 were unchecked before implementation pending independent code, database, integration, and design verification. Stacking remains unchanged: !323 targets TASK-433 at exact base `4e77d7db253d4f47a5ab128cea2e8dbcbe7a67b9`; !318 is open and conflicted. Remediation must remain TASK-440-only and must not retarget or merge either MR.

Comparison baseline failure-isolation slice completed in the dedicated TASK-440 worktree. Commit and generation baseline candidates now share lifecycle/schema/reference/content-shape/decode usability checks. Invalid commit baselines are removed without affecting the valid selected snapshot. Generation mode skips invalid retained predecessors and selects the nearest earlier usable generation. `query_options_page` revalidates the full baseline after selection and retries selected-only if a baseline decode fails during page loading, returning `comparison_available=false`, no baseline revision, no Changed count, and no before/diff/changed fields. Added one migration-backed SQLx regression for valid behavior plus corrupt, incompatible-schema, and missing-required-content baselines in both modes, including post-selection corruption, and enrolled it in `checks/server-regressions/default.nix`. Verification passed: `nix develop -c rustfmt --edition 2024 packages/default/crates/cf-server/src/queries/evaluation_snapshots.rs`; `nix develop -c env SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server --lib comparison_baseline_failures_are_isolated_for_commits_and_generations --no-run` (compiled; existing repository warnings only); `nix build .#checks.x86_64-linux.server-regressions -L --no-link` (full check passed, focused regression 1 passed); `git diff --check`. Only `evaluation_snapshots.rs` and `checks/server-regressions/default.nix` changed; migration 0237 and frontend files were not edited. No commit or push was performed.

Exported-module declaration pagination slice completed on the existing uncommitted comparison-baseline isolation baseline. Added `GET /api/v1/flakes/:id/revisions/:revision/modules/:module/declarations` with the existing viewer/environment non-disclosure policy, full-SHA validation, explicit lifecycle/error response, clamped 1-100 limit and 0-100000 offset, required continuation token, authoritative total, and deterministic ordering from one SQL statement over one persisted JSONB snapshot. The token is the complete flake-output content digest; a replacement returns `409 snapshot_changed`. The top-level output endpoint now keeps declaration totals/metadata but clears nested declaration arrays and emits `declarations_complete=false` when details exist. No migration was added and no MR/branch state changed.

The Modules UI now fetches page 1 only when a row expands, uses independent local loading/error/request-sequence state, preserves loaded rows during continuation, rejects token/offset/total/revision/module mismatches and overlaps, offers retry, and exposes a native keyboard-usable `Load more declarations` button. Backend/web DTOs and client methods, architecture/API docs, pure merge tests, exact DTO tests, and browser fixtures were updated. The browser fixture contains 125 declarations and proves keyboard continuation reaches row 125 with offset 100 and the page-one token.

Verification passed: `nix develop -c rustfmt --edition 2024 --check packages/default/crates/cf-server/src/bin/server.rs packages/default/crates/cf-server/src/handlers/api/flakes.rs packages/default/crates/cf-server/src/models/evaluation_snapshots.rs packages/default/crates/cf-server/src/queries/evaluation_snapshots.rs packages/web-ui/src/api/client.rs packages/web-ui/src/api/models.rs packages/web-ui/src/views/flakes_list.rs`; `nix develop -c node --check checks/web-ui/tests/integration-test.js`; `nix develop -c env SQLX_OFFLINE=true cargo check --manifest-path packages/default/Cargo.toml -p cf-server`; `nix develop -c env SQLX_OFFLINE=true cargo test --manifest-path packages/default/Cargo.toml -p cf-server module_declaration_read_requires_authentication_before_revision_disclosure` (1 passed); `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml` (271 passed, 1 ignored); `nix develop -c cargo check --manifest-path packages/web-ui/Cargo.toml --target wasm32-unknown-unknown`; `CF_UI_TEST_STEPS=13j-task440-flake-states-panes-navigation nix build --impure .#checks.x86_64-linux.web-ui -L` (focused browser step OK); `nix build .#checks.x86_64-linux.server-regressions -L` (all enrolled checks passed, including preserved baseline isolation and new 205-row declaration paging/token/no-mutation regression); `git diff --check`. Existing repository warnings remain. No commit or push was made.

Selected-revision Config parity verification (2026-08-30): `nix build .#checks.x86_64-linux.web-ui -L` returned exit 0. All TASK-440 browser workflows passed: `12l-task440-config-lifecycle`, `12m-task440-config-explorer-keyboard-wide`, `12n-task440-config-narrow-keyboard`, `13j-task440-flake-states-panes-navigation`, `13k-task440-drawer-modal-keyboard-layering`, `12o-task440-rollback-notification-auto-latest`, and `14d-task440-cross-surface-auth-navigation`. This evidence covers comparison rendering, selected-revision Config summaries/drift/provenance, declaration continuation beyond 100 rows, unavailable/error/non-disclosure states, exact cross-navigation, keyboard behavior, and 1920x1080 plus 900x900 layout assertions. The mega harness also printed failures in unrelated legacy workflows while still returning success and declaring the integration suite passed; those unrelated workflows are not claimed as verified here. AC #24 remains unchecked because this run did not independently prove both light and dark visual-theme parity. Final hygiene: all touched Rust files passed direct `rustfmt --edition 2024 --check`; `node --check checks/web-ui/tests/integration-test.js` and `git diff --check` passed. Workspace-wide `cargo fmt --all -- --check` remains blocked by pre-existing drift in untouched `packages/default/crates/cf-server/src/api/models.rs` and `packages/web-ui/src/components/flake/flake_timeline.rs`. No new TODO/FIXME lines were introduced. Changes remain uncommitted on the dedicated TASK-440 branch, and task status remains In Progress.

Theme/layout closure (2026-08-30): added explicit `dark` and `light` 1920x1080 geometry assertions to the TASK-440 Config and flake workflows. Config checks enforce viewport containment, aligned non-overlapping main/sidebar cards, and no unintended table inner scroller. Flake checks wait for the actual drawer animation to finish and enforce viewport containment in both themes. Existing 900x900 Config and flake assertions continue to enforce no horizontal clipping and accessible controls. The first run correctly failed because geometry was sampled during the 220 ms drawer animation; the test now waits on `Animation.finished` instead of using an arbitrary delay. Final `nix build .#checks.x86_64-linux.web-ui -L` returned exit 0 and all seven TASK-440 workflows passed. `node --check checks/web-ui/tests/integration-test.js` and `git diff --check` also passed. All acceptance criteria are now checked, but the task remains In Progress because the working changes are uncommitted/unpushed and MR !323 has not been updated or merged. The harness still reports unrelated noncritical legacy workflow failures before declaring the mega integration passed.

Acceptance-state correction after implementation delegation (2026-08-30): the delegated implementation prematurely checked AC3, AC9, AC15, AC23, and AC24 based on focused tests. Those criteria are reopened again pending independent diff inspection, real database/server integration review, design audit, full repository verification, and exact-head CI. Mocked browser workflows are not treated as end-to-end proof.

Backend P1 remediation completed without migration, frontend, browser, commit, push, or MR-stack changes. Baseline usability now uses a database-side corpus integrity predicate and bounded typed row decoding instead of fetching/deserializing the full baseline in Rust; generation fallback selects the nearest usable retained snapshot in one statement. A 5,001-option regression proves bounded 25-row paging and complete counts. Selected summaries return lifecycle-only data for failed or corrupt snapshots. Tracked repo URLs are sanitized at DTO construction while SQL exact matching retains stored values; self provenance resolves only when source_revision equals the selected full SHA. Declaration paging validates the complete declaration array server-side and converts malformed rows to unavailable/corrupt instead of an error/HTTP 500. Added unauthenticated summary/declaration tests and authenticated hidden-environment non-disclosure coverage for options, summary, and declarations. Focused regressions remain enrolled only in server-regressions.

Verification 2026-08-30: rustfmt and rustfmt --check passed for the four touched Rust files. `SQLX_OFFLINE=true cargo check --offline --manifest-path packages/default/Cargo.toml --package cf-server` passed with existing warnings. Authentication-focused unit tests passed (3/3). Full offline lib tests passed with `DATABASE_URL` unset: 1,285 passed, 0 failed, 408 ignored; an initial run inherited a read-only dev-shell DATABASE_URL and failed only three unrelated CVE tests on table permissions, then the repository-appropriate no-live-DB run passed. `nix build .#checks.x86_64-linux.server-regressions -L` passed end-to-end in 17m21s, including all focused migration-backed TASK-440 regressions. `git diff --check` passed. No migrations were changed and SQLx offline metadata was not changed because all new queries use runtime `sqlx::query`/`query_scalar`. Existing uncommitted frontend/browser/docs work was preserved.

Final remediation evidence (2026-08-30): independent review confirmed no remaining P1/P2/P3 findings after commit-mode retained-generation isolation, responsive Inputs source/metadata preservation, and stable module ARIA IDs were corrected.

Exact remediation commit `cafc678e` passed `nix build .#checks.x86_64-linux.server-regressions --no-link -L`, the focused seven-workflow TASK-440 browser gate, and `nix flake check --keep-going -L`. The focused gate captured all seven TASK-440 workflows successfully, including 1920x1080 dark/light and 900x900 coverage.

The unfiltered `nix build .#checks.x86_64-linux.web-ui --no-link -L` derivation exited successfully and all seven TASK-440 workflows passed, but its internal report listed unrelated legacy workflow failures while still returning success. Recorded the false-green harness defect as TASK-442; do not cite the unrelated legacy workflows as passing.

Browser TASK-440 workflows remain explicitly mocked-data coverage. Real behavior contracts are additionally covered by the isolated PostgreSQL/server-regressions check. MR !323 remains stacked on !318 and is not final merge-ready until !318 lands and the TASK-440-only diff is rebased, reviewed, and retested.

Published exact remediation head `cafc678e0c34aca4c6bf548ec1e97e3488a08e13` to `origin/TASK-440-system-config-flake-parity` without changing stacked MR !323's target. GitLab pipeline 2803150957 is running for that exact SHA: integration and oidc-auth are running; web-ui, server-regressions, state-machine, dashboard-visibility, and CVE jobs remain pending as of the latest poll. Pipeline URL: https://gitlab.com/crystal-forge/crystal-forge/-/pipelines/2803150957

Fresh maintainer review remediation opened from exact reviewed commit `cafc678e0c34aca4c6bf548ec1e97e3488a08e13`. AC9 is unchecked because selected-evaluation module/source rows are currently unbounded. AC23 is unchecked pending bounded Modules loading/error/retry/race/continuation browser coverage. AC24 is unchecked because the authoritative criterion still requires parity and the documented differences remain unresolved.

The contractual TASK-440 design target remains commit `eb5a18513623890e9dac1e8a74565078243288a8` (parent `cfae4f4a33815c72059d309a547672f8c9039747`) at the design paths already listed in task metadata. Current `dev` design changes do not silently replace that target. The comparison must be repeated against this exact target after MR !318 lands and !323 is reconciled.

Backend remediation completed in the dedicated TASK-440 worktree without commit or push. SelectedEvaluationSummary is scalar with module_source_total; GET /api/v1/systems/:id/evaluation-module-sources provides bounded C-collated source paging; one private set-based resolver decorates bounded module rows and selected/baseline option definitions; response-only tracked identities are cleared before persistence and repository URLs are sanitized. Snapshot module_count now counts exact redacted (source_input, source_revision, source_path) tuples, and additive migration 0238 backfills existing rows without indexes. Verification: targeted rustfmt and git diff --check passed; SQLX_OFFLINE cargo check --offline --package cf-server passed; cf-server lib test target compiled with --no-run; focused tracked identity/auth/bounds tests passed; `nix build .#checks.x86_64-linux.server-regressions -L` passed twice, with the final run after the redaction-count and empty-backfill corrections (7m27s). The enrolled summary/provenance test and 2,050-source paging/read-only/fixed-query-count test each passed. Existing repository warnings remain. Concurrent packages/web-ui changes appeared in the worktree during this slice and were not edited or reverted.

Focused frontend remediation completed in the task worktree without editing backend or browser files. Evaluation module-source DTO/client now carry the server snapshot token; continuations send it and collection merge rejects missing/changed tokens before mutation. Config uses one selected-scope refresh generation and one guarded queued/running poll timer for options, summary, and module page zero. Queue completion is bound to exact revision/mode/generation/refresh generation; selection changes clear expanded/source state. Added keyboard-native transport retries, specific continuation accessible names, and baseline priority/winner-note rendering. Verification: targeted rustfmt passed; `cargo test ... module_source` passed 4 tests; `cargo test ... refresh_scope_matches` passed 1 test; wasm32 cargo check passed; scoped `git diff --check` passed. Existing repository warning volume remains. No commit or push.

Focused backend pagination closure implemented in the user-authorized files. `persist_available_snapshot_tx` now redacts and bounds every option before it derives `option_count`, exact distinct provenance-tuple `module_count`, digests, search text, and persisted payloads. Migration 0238 adds a monotonic `snapshot_version` trigger and retains the exact tuple-count backfill. Evaluation module-source page one returns a 64-hex token derived from snapshot UUID plus persisted version; offsets above zero require a valid token; replacement mismatch maps to typed `snapshot_changed` HTTP 409. Integrity, token, total, bounded rows, and tracked provenance execute in one read-only repeatable-read transaction, so provenance cannot cross replacement versions. Added handler validation coverage and an isolated PostgreSQL regression covering oversized definition removal, count zero, same-row replacement, token change, stale continuation conflict, and fresh tracked provenance. The existing 2,050-source migration-backed paging/read-only/query-count regression also passes after accounting for the transaction consistency statements. Verification: targeted rustfmt passed; `nix develop -c env SQLX_OFFLINE=true cargo check --offline --package cf-server --lib --bin server` passed with pre-existing warnings; focused handler and oversized-option unit tests passed; both focused ignored PostgreSQL tests passed against temporary isolated PostgreSQL 17 instances; `git diff --check` passed. No commit or push. Frontend files were not edited; API consumers must mirror/send the new token in their separately owned frontend changes.

Browser module-source fixture slice completed in `checks/web-ui` only. Updated the Config summary fixture to emit scalar `module_source_total` with no embedded modules; added deterministic 86-source routed pagination with stable tokens, bounds/token validation, tracked/untracked exact tuple identities, lifecycle states, request holds, independent HTTP/transport failures, continuation failures, and replacement conflicts. Extended mocked TASK-440 workflows for initial 40-of-86 loading, preserved rows and same-offset retries, direct option provenance before module pages resolve, delayed revision isolation, and 900x900 keyboard load-more/retry. Verification: `node --check checks/web-ui/tests/integration-test.js` passed; custom manifest parity check passed with 156 workflows; `git diff --check -- checks/web-ui` passed. Focused authoritative Nix browser execution passed: final run of 12l and 12m passed, and 12n passed in the immediately preceding focused run after its final relevant edit. The first focused run exposed and led to correction of deterministic transport/provenance assertions; a second run exposed and led to correction of transport arming and a stale vertical geometry assertion. No production code/docs were edited by this slice, and no commit or push was performed.

Remediation verification (2026-08-30): implemented scalar selected-evaluation summaries plus bounded replacement-safe module-source pagination. Migration 0238 backfills exact distinct `(source_input, source_revision, source_path)` totals and adds monotonic snapshot versions. The server validates continuation tokens, reads each page and tracked provenance in one read-only repeatable-read transaction, and returns HTTP 409 on replacement. The UI independently loads 40-row pages, restarts at page zero after replacement, preserves same-offset retry for transport errors, scopes queue/poll completion to the exact selection, clears stale trays, and uses only server-issued tracked identities. Updated API/architecture documentation and mocked authoritative browser fixtures. AC9 and AC23 are now supported by isolated PostgreSQL regressions and critical browser workflows. AC24 remains unchecked because literal design parity and authoritative visual baselines are not satisfied.

Verification completed on the combined remediation: `nix build .#checks.x86_64-linux.server-regressions --no-link -L` passed; `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml` passed 278 tests with 1 ignored before the final replacement-recovery adjustment; focused module-source tests passed 4/4 after that adjustment; WASM cargo check passed; `node --check checks/web-ui/tests/integration-test.js` passed; and final `nix flake check --keep-going -L` passed. The critical mocked workflows 12l, 12m, 12n, 13j, 13k, 12o, and 14d all passed in the final web-ui VM run. The harness still reported unrelated non-critical workflow failures while returning success; TASK-442 tracks that false-green behavior. A broad ambient `cf-server --lib` run compiled and passed 1,286 tests but had three unrelated CVE test failures caused by the current database user's missing table permissions; the isolated server-regressions derivation passed all enrolled TASK-440 database tests.

Published remediation commit `1b9a0594193a26da99b0935151b64323acf8f913` to `origin/TASK-440-system-config-flake-parity` and added exact verification/evidence to draft MR !323. The task remains In Progress and MR !323 remains stacked on TASK-433 / !318. Do not move to Review while AC24 is unsatisfied or before the stacked dependency is reconciled.

Exact-head GitLab pipeline `2803618829` started for SHA `1b9a0594193a26da99b0935151b64323acf8f913`: https://gitlab.com/crystal-forge/crystal-forge/-/pipelines/2803618829. At the final check it was running, with integration and OIDC jobs running and remaining jobs pending/created. Do not report exact-head CI as passed until this pipeline completes successfully.

AC24 authoritative backend contracts implemented in the dedicated TASK-440 worktree without frontend/browser/docs/protocol edits. Selected summary now returns deterministic same-commit modal host delta (absence participates; bytewise tie-break; single-config result 0), optional actual local recursive Nix toplevel closure bytes, exact selected/latest-agent store fingerprint, and fail-closed seven-day exact store-path drift with the existing offline threshold as the maximum coverage gap. Flake output adds paging-independent mismatch totals, all/declared_unmanaged/managed_undeclared filtering, and visibility-scoped environment metadata. The independent carrier adds sanitized optional module attribute-position provenance and direct-root transitive descendant counts; deltas add input additions/removals. Migration 0239 adds nullable closure_size_bytes only; existing indexes support the bounded seven-day query and an EXPLAIN assertion proves both are selected. Final verification passed: targeted rustfmt check; SQLX_OFFLINE cf-server check; focused pure redaction/delta/Nix-size tests; generated-expression parse; real local Nix module-source and transitive-input extraction; and `nix build 'path:.#checks.x86_64-linux.server-regressions' --no-link -L` including the new isolated PostgreSQL regression. No commit or push.

Independent AC24 backend diff review found and corrected two closure-metric integrity defects. Zero-dependency derivations now require the same successful recursive `nix path-info` query before recording bytes, and an unavailable repeat measurement no longer erases a previously persisted successful measurement. The AC24 migrated-PostgreSQL regression now proves this monotonic persistence contract. Verification after correction passed: targeted `rustfmt --check`; `SQLX_OFFLINE=true nix develop -c cargo check --manifest-path packages/default/Cargo.toml --package cf-server --lib`; closure JSON parser test (1 passed); `nix build 'path:.#checks.x86_64-linux.server-regressions' --no-link -L`, including AC24 database regression (1 passed); and `git diff --check`. Repository-wide cargo fmt remains blocked by an unrelated pre-existing import-order difference in `packages/default/crates/cf-server/src/api/models.rs`; no unrelated file was changed. AC24 remains unchecked and TASK-440 remains In Progress. No commit or push.

2026-08-30 authoritative metric contract and UI-consumption verification: Added persisted closure bytes, host delta, exact agent fingerprint, seven-day drift, server-issued reconciliation totals/filtering, environment metadata, module carrier provenance/direct descendant counts, and flake input addition/removal contracts. The Rust UI now consumes these contracts in Config and Flake surfaces without deriving unsupported values. Corrected closure measurement so a zero-dependency derivation still requires a successful recursive root query, and changed persistence so a later unavailable measurement does not erase a successful closure-byte value. Verification passed: targeted rustfmt; `SQLX_OFFLINE=true nix develop -c cargo check --manifest-path packages/default/Cargo.toml --package cf-server --lib`; `nix develop -c cargo check --manifest-path packages/web-ui/Cargo.toml --target wasm32-unknown-unknown`; full `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml`; focused backend DTO/query/helper tests; `nix build 'path:.#checks.x86_64-linux.server-regressions' --no-link -L`; migrated AC24 PostgreSQL regression including monotonic closure-byte persistence; authoritative browser workflows `12m-task440-config-explorer-keyboard-wide`, `12n-task440-config-narrow-keyboard`, and `13j-task440-flake-states-panes-navigation`; `node --check checks/web-ui/tests/integration-test.js`; and `git diff --check`. Repository-wide `cargo fmt --all -- --check` remains blocked by an unrelated pre-existing import-order difference in `packages/default/crates/cf-server/src/api/models.rs`. AC24 remains unchecked because the complete 1920x1080 light/dark and 900x900 visual-parity evidence has not yet been captured. No commit or push was performed.

Config summary metrics correction completed in the dedicated worktree. Migration 0239 now adds optional `derivations.closure_size_bytes` and the covering `idx_system_states_hostname_timestamp_identity` observation index. The seven-day query uses the pre-window observation only for boundary coverage, not as in-window drift; the focused PostgreSQL regression proves incomplete coverage fails closed, continuous matching coverage reports no drift despite a differing pre-window boundary state, and an in-window exact store-path change reports drift. Verification passed: touched-file rustfmt check; `SQLX_OFFLINE=true cargo check --package cf-server --lib --offline`; closure-size JSON parser unit test; `nix build 'path:.#checks.x86_64-linux.server-regressions' --no-link -L` including `ac24_metrics_and_filtered_reconciliation_are_authoritative`; and `git diff --check`. The repository-wide cargo fmt check remains blocked by a pre-existing unrelated import-order difference in `cf-server/src/api/models.rs`; that file was not changed. No commit or push.

Backend flake-contract closure (2026-08-31): preserved the existing Config metric/frontend/migration work and wrote only cf-server plus the already-enrolled server-regressions scope. The flake output API now has typed reconciliation filtering, revision-global mismatch totals, visible environment metadata, and complete filtered continuation coverage. Exact lock-node input additions/removals are represented separately from revision bumps. The configuration-independent Nix carrier records optional module binding source input/revision/normalized relative path from `builtins.unsafeGetAttrPos`, keeps declaration source paths separate, and emits authoritative `transitive_descendant_count` from the full lock graph while retaining `direct_descendant_count` for the current frontend consumer. Verification passed: targeted rustfmt checks; `SQLX_OFFLINE=true cargo check --offline --package cf-server --tests`; focused delta, path normalization, and real-Nix carrier tests; `git diff --check`; and `nix build path:.#checks.x86_64-linux.server-regressions -L`. The ordinary flake-form server-regressions invocation failed because Nix excluded the preserved untracked 0239 migration and the test database lacked `closure_size_bytes`; `path:.` included that migration and the full check passed. No commit or push.

Frontend AC24 continuation (2026-08-30): final audit found and fixed a production-only stale helper reference that blocked the Dioxus WASM bundle. `cargo test` passed (280 passed, 1 ignored), wasm32 `cargo check` passed, and touched-file rustfmt passed. The authoritative `nix build .#checks.x86_64-linux.web-ui --no-link` then reached Playwright. The first run exposed missing authoritative module-source total and exported-module carrier provenance; frontend markup was updated, and the next run advanced both workflows. Remaining TASK-440 browser failures are: Config pending search expects `.cfg-count` text `Loading…` rather than `Querying…`; flake declaration headers need explicit accessible columnheader semantics. Unrelated browser failures also remain in non-admin Sync-all visibility and onboarding/policy locator ambiguity. Work stopped because all worktree `.git` files now point to removed `/home/mcamp/code/crystal-forge/refactor/.git/worktrees/*` metadata, so Git can no longer identify or inspect the TASK-440, dev, or main worktrees. Source files remain present. Do not move AC24 or TASK-440 to Review until worktree metadata is repaired and the authoritative web-ui check passes.

Backend latest-review remediation committed and pushed as `4e09d60a` on 2026-08-31. Scope: cf-server backend, migration 0239, and checks/server-regressions only; frontend/browser worktree changes remain preserved and uncommitted. Materialized same-commit host delta, fixed four-hour drift coverage, set-based visibility-aware fleet reconciliation and globals, export-binding provenance semantics, direct/transitive input counts, duplicate-index removal, bounded closure JSON collection, first-page-only revision deltas, and AgentFingerprint documentation. Verification passed: rustfmt; SQLX_OFFLINE cf-server check; 18 focused evaluation snapshot tests; focused closure parser and binding ambiguity tests; real-Nix independent carrier and exported-module tests; cf-server rustdoc build (existing warnings only); `git diff --check`; and `nix build path:.#checks.x86_64-linux.server-regressions -L`. The Nix check includes a 1,003-system query-count/index-plan regression and a 128-configuration by 1,000-option host-delta query-count/index-plan regression.

2026-08-31 frontend Config/Flake parity closure: modified only packages/web-ui and preserved backend HEAD 4e09d60a plus existing checks/web-ui diffs (numstat remains 96/0, 8/0, 368/17). Config now retains/dims the prior available page during background debounce/query, guards both raw and debounced query identity, shows Querying, and supports clear/continuous typing. Measured page sizing now sums natural child card rectangles and rendered inter-card gaps rather than the stretched side wrapper, retaining formula/clamp/hysteresis/offset reset. Source tray shows locked provenance metadata and explicitly says source content is unavailable; Evaluation uses cached. Flake DTOs consume backend global warning counts, systems continuation, and transitive descendants; continuation is active-pane-local, merges deduplicate collections/global deltas, missing module binding is unavailable, follows strings render unquoted, and full SHA remains title/accessible identity. Ported remaining Config/Flake dimensions from eb5a185 and added Node/NodeList web-sys features. Verification: targeted rustfmt completed; `nix develop -c cargo test --manifest-path packages/web-ui/Cargo.toml` passed 283 tests with 1 ignored; targeted exact flake DTO test passed; `nix develop -c cargo check --manifest-path packages/web-ui/Cargo.toml --target wasm32-unknown-unknown` passed; `git diff --check` passed. `nix build .#checks.x86_64-linux.web-ui --no-link` was attempted twice: dependencies built and the final VM integration derivation started, but both runs exceeded tool limits (20 minutes then 10 minutes) and were terminated without a pass/fail result. No commit or push.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: openai-agent
created: 2026-08-28 16:42
---
Parallel-work coordination (2026-08-28): TASK-440 may be implemented in parallel with TASK-433, but TASK-440 must not merge until TASK-433 is merged. Before TASK-440 adds any database migration or refreshes SQLx metadata, inspect TASK-433's latest branch state and migration numbers, then rebase/update from post-TASK-433 `dev` and allocate new additive migration numbers. Known overlap includes System Detail/Compliance UI, server models/queries/handlers, migrations, SQLx metadata, and browser checks. Preserve TASK-433's POA&M behavior when resolving overlap.
---
<!-- COMMENTS:END -->
