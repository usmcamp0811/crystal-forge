# Web UI Baselines

This directory contains reviewed visual baselines for strict manifest steps.
Generate captures through the Nix check and approve them with
`checks/web-ui/approve-baselines.sh`. Commit the resulting PNG files.

Do not add failed-step diagnostics, diff images, reports, or export-test
screenshots to this directory.
